# 兼容性矩阵

**简体中文** · [English](../en/compatibility-matrix.md)

状态取值：`unassessed`（未评估）、`planned`（已规划）、`partial`（部分实现）、`compatible`（兼容）或 `intentionally-different`（有意不同）。“证据”指向固定版本的 Rust 源项目。

| 能力 | 目标 | 状态 | 证据 / 备注 |
|---|---|---|---|
| 软件包与 CLI 组合根 | M1 | partial | 普通软件包已包含 Textual，并发布等价的 `neuro` 和 `neuro-code` 控制台脚本；`neuro code` 是显式 TUI 别名，三者都把启动目录作为工作区，全局工具安装后无需每次激活虚拟环境。CLI、TUI 和 ACP 共享与界面无关的 `ApplicationComposition`；独立原生安装程序仍待实现 |
| `version` 与 JSON 版本输出 | M1 | partial | 已实现独立软件包元数据 |
| 生效配置检查 | M1 | partial | `neuro-code inspect`；已实现隐藏密钥的 Python 视图 |
| 命名供应商 profile 与选择 | M2/M3 | partial | 已实现四种显式线路协议、默认/单次选择、旧 TOML 兼容、脱敏的 `providers list/inspect`、首次启动 TUI 设置、分类优先的设置流程、多 profile 新建/编辑/保存并使用/二次确认删除、显式 OpenAI Responses/兼容 Chat/DeepSeek 预设、由用户触发的连接测试与有界内存远程模型选择器及手动回退，以及通过 `/provider`、`/model` 和 Ctrl+P 选择并持久保存受管默认项。通用 Responses 适配器只在 `neuro_code.infrastructure.providers.openai_responses.OpenAIResponsesProvider` 中实现；xAI 仍是 `openai-responses` 方言。Architecture Freeze v1 兼容性清理已移除过时的 `neuro_code.providers` 包及其 Provider 子模块 facade；调用方应使用规范 infrastructure Provider 路径。元数据与 API 密钥经端口分别写入原子私有文件；已保存值不进入对象表示，并在工具结果边界脱敏。managed JSON reader 只在 `neuro_code.configuration.managed_provider_settings` 公开，`JsonProviderSettingsStore` 仍是 adapter API；移除 loader re-export 与 `ProviderConfig` alias 后，`ProviderProfile`/`AppConfig` 仍是 config 类型。同名受管 profile 会完整替换 TOML 表以阻止工作区重定向端点，删除会一并移除 profile 元数据及其凭据条目并安全选择剩余默认项。操作系统钥匙串和持久化/离线模型目录缓存仍待实现 |
| 类型化 Provider 投影接缝 | M3 | compatible | `ProviderOption` 与 `ProviderSelectionResult` 由 `neuro_code.application.providers.contracts` 拥有；Provider 应用服务、bootstrap 和 TUI 使用规范接缝，而 profile/session 与旧 runtime 导入保留保持 identity 的兼容 re-export。Provider 生命周期与选择行为保持不变 |
| CC Switch 兼容 | M2 | partial | 已实现只读映射 `NEURO_CODE_CC_SWITCH_CONFIG`、三种 backend 格式、回环 `PROXY_MANAGED`、环境变量引用、内联密钥拒绝和项目配置覆盖；仍不包含 CC Switch 数据库/进程控制及其内部故障转移 |
| 安全供应商故障转移 | M2 | partial | 已实现有序按需备用 profile、第一个事件提交边界、单次运行单向选择、可审计失败/选择事件、`--no-failover`、汇总错误和不透明会话来源保护；重试、熔断和持久化健康状态仍待实现 |
| 供应商 HTTP 代理策略 | M2/M3 | partial | 已实现按 profile 配置环境/直连/显式环境变量模式、TUI 编辑与保存前校验、受管默认项启动失败的 TUI 内恢复、四适配器统一 HTTPX 选项、检查/错误脱敏、含义不明 SOCKS scheme 的严格诊断，以及 `socks` 可选安装 extra；保留无效继承 `ALL_PROXY` 时，显式路由已通过 DeepSeek 手动验证。PAC、多代理挂载和默认捆绑 SOCKS 支持仍待实现 |
| 无头单次提示 | M2 | partial | 已实现流式文本/工具循环，以及纯文本、JSON、JSONL 输出 |
| 应用自有思考强度 | M3 | partial | 已实现供应商中立的 `low`/`medium`/`high`/`xhigh`/`ultracode`、默认 `high`、仅请求生效的审查指引、有类型的 `ModelContext` 传递、`--effort`、TUI Ctrl+E 与 `/effort`/`/reasoning`、请求/实际等级显示及原子用户偏好持久化。当前适配器不会发送供应商私有强度参数；`ultracode` 明确回退到 `xhigh`，也不会启动子代理工作流。按能力启用的原生映射以及工作流/子代理编排仍待实现 |
| 交互工作模式 | M3/M4 | partial | 已实现应用自有 `normal`/`accept-edits`/`plan`/`auto`、`Shift+Tab` 循环、`/mode MODE` 补全、常驻显示、原子偏好持久化、profile/会话重新应用、仅请求生效的模型指引和确定性权限模式映射。计划模式现在具有有界、供应商中立的 `update_plan` 工具、持久会话恢复/分叉继承、本地化的 TUI `/plan DESCRIPTION` 和 `/view-plan`/`/show-plan`，以及绑定到当前计划修订的本地 `/comment-plan STEP COMMENT` 反馈；显式 `/execute-plan`/`/run-plan` 交接会在规范执行请求之前记录持久事件，并在一条不透明会话任务生命周期中保存不可变计划修订快照，随后只切换到 `accept-edits`。已实现有界显式 `/schedule-plan`/`/queue-plan` 持久化和 `/run-task TASK_ID` 认领，每会话最多四个排队任务；排队任务不会自动启动或创建子代理。除非启动时显式授权 `--always-approve`，`auto` 会标为采用 accept-edits 默认值的安全预览；模型安全分类器仍待实现，显式规则和进程沙箱始终有效 |
| 轮次与工具计时反馈 | M3 | partial | 已实现每个模型步骤的单调时钟首个可行动结果耗时、整轮完成/失败耗时及本地/托管工具耗时。每次调用保留稳定 call-ID 状态，连续活动则显示为一个带有界聚合摘要的折叠组。单击或 Enter 打开固定高度、只含一个调用的 Inline Peek；再次单击已打开的 Peek 或按 Esc 会将其收起，焦点移动后也有效。上/下方向键选择调用，Enter 打开其 Output/Input/Meta Inspector。Inspector 会跟随实时生命周期更新而不阻塞 Conversation；运行计时器对每个 Summary 组最多刷新一次，并跳过已打开的 Peek/Inspector 布局。Conversation 永不加载 artifact 输出，Inspector 则通过既有属于当前会话、256 KiB 且已脱敏的读取边界懒加载并明确提示截断。Transcript Copy 不受 disclosure 状态影响。这些耗时是客户端观测，不是供应商私有推理遥测；持久化完整工具记录仍待实现 |
| 上下文窗口预算与用量显示 | M3 | partial | 已实现可选的 profile 正整数 `context_window_tokens`、脱敏选择/故障转移传播、启动/恢复估算、供应商报告的输入加输出用量事件、紧凑可本地化的状态/提示显示、`/status` 报告及 50%/80% 警示配色。该字段是本地元数据，不会作为 API 参数发送；精确的供应商请求前分词仍待实现 |
| OpenAI 兼容/xAI Chat 端点 | M2 | partial | 已实现 Chat Completions 流式响应、有界 `max_tokens`、工具、经过校验的原生用户图片块、思考模式工具往返连续性，以及失败关闭的上游导入可见上下文回放；DeepSeek V4 DSML 解析仅由显式 `openai-chat` `dialect = "deepseek-v4"` profile 启用，双门禁在线流式/故障转移/读取工具测试已实现并手动通过，仍需可提交的模型专用录制夹具 |
| OpenAI Responses/xAI 方言 | M2 | partial | 已实现通用可移植 Responses 子集，以及可选 xAI 加密推理、网页/X/代码托管工具、profile 亲和原生回放、严格旧官方主机回退、终态权威输出、SSE 生命周期统一、状态剥离、回退修复和错误脱敏；仍需托管工具高级筛选、有状态 response ID、压缩项和选择性在线夹具 |
| Anthropic 提供商 | M2 | partial | 已实现原生 Messages SSE、文本/推理、工具、用量、错误、密钥脱敏，以及经过校验的用户/工具结果图片块；仍需在线/模型专用夹具 |
| Gemini 提供商 | M2 | partial | 已实现原生 `streamGenerateContent` SSE、文本/思维、工具、用量、错误、thought-signature 往返，以及经过校验的用户内联/File API 图片项；仍需在线/模型专用夹具 |
| 核心工具注册表与模式 | M2 | partial | 已实现独立 Python 契约，以及按能力启用的后台 Bash/任务输出/多任务等待/终止 schema |
| 读取/列举/grep 工具 | M2 | partial | 在工作区内 UTF-8 单文件兼容能力之外，已实现有序有界 `read_files`、拒绝链接并过滤生成目录的 `list_tree`，以及确定性多查询 `grep_many`；批量输出会脱敏并受字节限制，部分读取失败仍可用，工具调用继续顺序执行 |
| 搜索/替换编辑 | M2 | partial | 已实现原子精确替换与路径边界检查 |
| Bash 执行与取消 | M2/M3 | partial | 已实现有界前台输出、空标准输入、已配置供应商/代理凭据剥离、超时/取消清理、POSIX 进程组 TERM/KILL、失败关闭的 Windows Job Object 创建与原子 `PROC_THREAD_ATTRIBUTE_JOB_LIST` 加入/受限标准句柄继承/后代等待/整 Job 终止、会话作用域受管后台启动/快照/有界单任务与多任务事件等待/超时/幂等终止/绑定切换/应用关闭生命周期、带任务输出/多任务等待/终止去重的有界模型专用完成提醒、重启感知的有界会话唤醒账本，以及复用同一进程树的前台自动转后台；持久全局默认、供应商覆盖和会话级 `/auto-wake on|off` 优先级已实现；已通过可选状态目录存储为截断的本地 Bash 预览提供有界脱敏完整输出文件，显式审查后的密钥注入以及 ACP 客户端终端/文件读取器的产物支持仍待实现；明确的自动唤醒可注入每任务 2 KiB、每批 8 KiB 的脱敏输出预览 |
| 权限规则优先级 | M2/M3 | partial | 已实现 deny/ask/allow 优先级、逐片段检查 `&&`/`||`/`;`/管道、包装器剥离、嵌套 `bash -c`、复杂脚本失败关闭、异步单次允许/拒绝、仅保存在内存中的精确操作会话批准，以及每个 ACP session 独立的 `session/request_permission` 适配；本地 deny/工作区/沙箱结论始终拥有最终优先级，客户端审批前会先发送 pending 工具 update。审批请求/响应契约现在只可从 `neuro_code.application.permissions.contracts` 获得；根级 `neuro_code.permissions` 兼容 facade 已移除，策略调用方应使用 `neuro_code.application.permissions.policy`。仍需持久化审查规则和完整规则/文件访问语法 |
| SQLite 会话事件存储 | M2/M3/M4/M5 | partial | 已实现 schema v13、原子且会串行处理并发初始化器的 v1/v2/v3/v4/v5/v6/v7/v8/v9/v10/v11/v12 迁移、profile 亲和与固定沙箱元数据、稳定标题、同步 FTS5 文档、带 namespace 的一对一 external session alias、有界持久结构化计划、排除 FTS/导出/导入且在替换计划时清理的有界当前计划评论、携带不可变计划快照且排除 FTS/导出/导入/分叉复制的持久不透明计划执行任务记录、每个源会话最后一个安全终态执行记录、有界会话级后台唤醒账本、带外键的父子 `subagent_links`，以及有界且带外键的 `session_compaction_items`。压缩条目只保存 Provider/窗口元数据、源计数与范围、不透明过期源指纹、摘要 token 元数据、创建时间和已脱敏摘要；不进入 FTS/导出/导入或分叉复制。执行记录绑定已持久化的 `TURN_COMPLETED` 事件，只保存状态、reason code、finalized/recoverable 标志、序号和时间戳；运行时终态成功会通过一个 SQLite 事务同时提交该事件、最终有序会话项、同步标题/FTS 以及可选记录；后台自动唤醒省略记录，因此会保留此前用户记录。执行记录写入会串行处理，并拒绝更旧的事件序号或同序号冲突数据。压缩条目写入是独立短事务，不是整轮原子性。该边界不是整轮或跨进程原子性；执行记录、压缩条目和子代理链接都不进入 FTS、导出/导入或分叉复制。还实现了限当前会话的精确任务读取、immutable 只读恢复预读、规范有序会话项持久化、消息投影、只追加前缀检查、列表、恢复、分叉、导出、跨平台路径别名的文件系统身份工作区匹配，以及删除父会话时递归删除关联子会话 |
| 会话标题与全文搜索 | M3 | partial | 已实现首条可见用户提示的确定性十词标题、上游 `generated_title` 保留、通过 `sessions rename` 和 TUI `/rename`/`/title` 原子更新手动标题与 FTS、标题十倍权重的 SQLite FTS5、多词 AND 到 OR 回退、cwd 过滤、分页、命中字段、可选摘要、`sessions search` 和 TUI `/sessions QUERY` 工作区结果；系统消息、私有推理/原生保留项、工具参数/原始结果和图片 URL 不进入索引。实时防抖会话选择器搜索已实现；模型生成标题和 ACP 方法仍待实现 |
| 类型化会话回合应用边界 | M3 | compatible | `RunTurnRequest`、`SessionTurnRunner` 和 `SessionTurnService` 由 `neuro_code.application.sessions.turns` 拥有；CLI、TUI、ACP 和应用消费者使用规范接缝，同时 package 与旧 session service 保留保持 identity 的兼容 re-export。回合锁、持久化上下文、取消、事件顺序和 Runtime 行为保持不变 |
| 类型化会话绑定应用边界 | M3 | compatible | `ConversationBinding` 与 `ConversationRunner` 由 `neuro_code.application.sessions.binding` 拥有；ACP、bootstrap、会话应用服务以及面向 Runtime 的消费者使用规范接缝，profile 与旧 runtime 路径保留保持 identity 的兼容 re-export。profile 专属会话选择与绑定替换、回合锁、取消和 Runtime 行为保持不变 |
| 类型化会话选择与策略投影 | M3 | compatible | `SessionOption`、`SessionSelectionResult`、`ReasoningEffortSelectionResult` 和 `InteractionModeSelectionResult` 由 `neuro_code.application.sessions.contracts` 拥有；TUI 消费规范投影，profile 与旧 runtime 路径保留保持 identity 的兼容 re-export。选择、策略应用、锁、恢复和 wire 行为保持不变 |
| 类型化交互式会话选择接缝 | M3 | compatible | `SessionSelectionController` 与非拥有型 `SessionSelectionService` 由 `neuro_code.application.sessions.selection` 拥有；TUI 通过门面执行列表/搜索/选择/重命名，`ProfileConversationController` 继续负责锁、绑定替换、工作区校验、恢复生命周期及兼容性的执行记录投影。会话 schema、Runtime、Provider、Finalizer 和 wire 行为保持不变 |
| 类型化持久会话生命周期边界 | M3 | compatible | `StartSessionRequest`、`ImportSessionRequest`、`RenameSessionRequest`、`ForkSessionRequest`、`DeleteSessionRequest` 与 `SessionLifecycleService` 由 `neuro_code.application.sessions.lifecycle` 拥有；Runtime 会话创建、CLI 导入/重命名以及 ACP 分叉/删除使用规范请求，旧 session service 通过保持 identity 的兼容导出进行委托。工作区可见性、binding 替换、回合锁、协议清理、schema、Runtime 执行、Provider、Finalizer 和 wire 行为保持不变 |
| 类型化会话任务查询边界 | M3 | compatible | `ListSessionTasksRequest`、`GetSessionTaskRequest`、`SessionTaskQueryController` 与 `SessionTaskQueryService` 由 `neuro_code.application.sessions.task_queries` 拥有；Runtime 排队计划查询和 `AgentConversation` 任务读取使用规范接缝，旧 session service 通过保持 identity 的兼容导出进行委托。任务写入、状态转换、锁、取消、Runtime、Provider、Finalizer、schema 和 wire 行为保持不变 |
| 类型化会话摘要查询边界 | M3 | compatible | `GetSessionSummaryRequest`、`SessionSummaryQueryController` 与 `SessionSummaryQueryService` 由 `neuro_code.application.sessions.summary` 拥有；会话恢复、bootstrap 配置、ACP 工作区校验和会话作用域工具输出 artifact 读取使用规范接缝，旧 session service 通过保持 identity 的兼容导出进行委托。生命周期写入、事件/会话项读取、schema、事务、Runtime、Provider、Finalizer 与 wire 行为保持不变 |
| 类型化执行记录查询边界 | M3 | compatible | `LoadExecutionRecordRequest`、`LoadExecutionRecordsRequest`、`SessionExecutionQueryController` 与 `SessionExecutionQueryService` 由 `neuro_code.application.sessions.execution_queries` 拥有；会话目录和会话恢复/重载读取使用规范的单条/批量接缝，旧 session service 保留保持 identity 的导出。执行记录写入、schema、事务、Runtime、Provider、Finalizer、TUI、ACP 与 wire 行为保持不变 |
| 类型化会话事件查询边界 | M3 | compatible | `LoadSessionEventsRequest`、`SessionEventQueryController` 与 `SessionEventQueryService` 由 `neuro_code.application.sessions.event_queries` 拥有；会话导出和会话作用域工具输出 artifact 读取共享复制后的不可变 mapping 投影，旧 session service 保留保持 identity 的导出。事件写入、解码、schema、事务、Runtime、Provider、Finalizer、TUI、ACP 与 wire 行为保持不变 |
| 类型化会话项查询边界 | M3 | compatible | `LoadSessionItemsRequest`、`SessionItemQueryController` 与 `SessionItemQueryService` 由 `neuro_code.application.sessions.item_queries` 拥有；会话恢复/重载和会话导出共享规范的有序 tuple 投影，旧 session service 保留保持 identity 的导出。会话项写入、计划、评论、事件、schema、事务、Runtime、Provider、Finalizer、TUI、ACP 与 wire 行为保持不变 |
| 规范 application 消费者导入 | M3 | compatible | Bootstrap composition 与 TUI 从具体的 `application.providers.service` 和 `application.workflows.*` owner 导入 Provider/工作流服务；CLI/bootstrap/ACP 与 CLI 序列化器从具体的 session lifecycle/service/catalog owner 导入。聚合 package export 仍保持 identity 的兼容路径。本阶段只改变导入，不改变 service 所有权、锁、持久化、Runtime、Provider、Finalizer、TUI 布局、ACP wire 或工作流行为；由于 plan/comment/export 没有第二个生产消费者，仍由现有 owner 负责 |
| 规范工具输出 artifact 导入 | M3 | compatible | CLI、TUI、ACP、bootstrap、ACP application 编排和 CLI 序列化器直接从 `neuro_code.application.tools.service` 导入有界 artifact application owner；package 聚合入口仍是保持 identity 的兼容路径。artifact 句柄、会话可见性、脱敏、字节上限、清理、权限、存储、Runtime 与协议行为保持不变 |
| Rust 会话导入 | M2 | partial | 已实现只读解析 v0/v1、新旧消息混排、结构化图片、有序推理/后端工具载荷、内嵌 `raw_output`/单体推理恢复与后端 ID 去重、有界损坏行恢复、SQLite 原子导入、内建沙箱 profile 与生成标题保留、自定义 profile 拒绝、前缀安全恢复、导出 v4、供应商原生图片回放、可信来源的 xAI Chat 回放，以及严格亲和的 Responses 原生加密/后端工具回放；压缩与有状态 ID 仍待实现 |
| 全屏/精简 TUI | M3/M4 | partial | Textual 界面已实现有界多行 Composer、安全 Markdown、带滚动跟随保护的稳定原地流式回答、F8 会话复制界面，以及共享阅读轴且最多 116 列的对话块。紧凑中性语义主题只保留一种交互 accent；连续工具调用会形成一个默认折叠的活动组，摘要显示聚合数量、可见失败与耗时，并截断 Bash 意图；单击或 Enter 可查看有界脱敏的权限、完整命令、输出、artifact 与 diff。永久底部区域只保留 Composer，以及在有界模型/强度/模式和上下文/工作区区域之间分配空间的一行无冗余字段名状态；`/help` 与 F1 取代快捷键栏。克制设置与强度列表区分焦点和已选状态，Modal 使用小/中/大三档。既有五级强度、四种交互模式、计划/任务/后台唤醒流程、供应商/会话设置和发现、中英文界面、失败关闭审批、取消/重试/回退、视口校准、持久上下文、生命周期清理以及跨平台无头/生产终端测试继续保留。TUI 可在展开时读取会话作用域的有界 Bash 输出 artifact；其他输出来源的 artifact 仍待实现。详见 [ADR 0108](adr/0108-editorial-tui-presentation.md)。 |
| Markdown/Mermaid/媒体渲染 | M3/M5 | partial | 助手 Markdown 使用应用自有语义主题，不把模型文本解释为 Rich/Textual markup，并禁用链接点击；已渲染标题、强调、代码、列表、链接和表格。工具输出与差异使用应用赋予样式的字面文本。Mermaid 和内嵌媒体仍待实现 |

| PTY 与进程树对齐 | M3/M4 | partial | 前台/后台命令共享受控 `ProcessTree`；POSIX 会等待仍存活的进程组，Windows 则在关闭即终止的 Job Object 中原子创建入口、限制继承的标准句柄、在入口退出后等待后代，并使用基于句柄的整树终止。新的共享交互式终端领域/端口和管理器提供有界且按游标寻址的尾部输出、显式截断、输入/resize/信号/等待/关闭、权限/工作区/沙箱门禁、取消安全创建与注册表关闭。POSIX 会话持有 PTY 进程组；生产 Windows 会话原子组合 ConPTY 与 Job 列表属性，并终止整个 Job。可移植属性/句柄/错误测试、真实 Linux PTY 测试、原生进程树测试和选择性三平台 CLI 终端冒烟覆盖该边界。ACP 还支持由能力协商控制的非交互式客户端前台执行，以及有界标准直接可执行文件后台生命周期；不提供交互式 PTY framing/背压、输入或 resize |
| 操作系统沙箱配置 | M3 | partial | 已实现规范的 `off`/`workspace`/`read-only`/`strict` 配置与 CLI 覆盖、防止项目弱化用户 profile、针对 Bash/后台 Bash/stdio MCP/启用 profile 的 PTY 请求的 Linux 子进程范围 Bubblewrap、私有 child 状态与环境、有界 controller 状态硬链接拒绝、只读编辑 schema/运行时门禁、仅限 child 的网络/PID 命名空间、可信辅助程序预检、不支持平台的失败关闭、无 skip 的专用 Linux namespace CI、原生 Windows Job/ConPTY CI，以及会话固定 profile 保存/恢复冲突检测；`off` 明确不提供操作系统隔离；PR5 已删除 controller 范围重执行和命名空间 attestation；自定义 profile、`devbox`、deny glob、Landlock/Seatbelt 与 Windows 强制仍待实现 |
| ACP stdio/WebSocket | M4 | partial | 已通过官方 `agent-client-protocol==0.11.0` stdio SDK 实现协议版本协商、客户端能力/info 保存、绑定工作区且稳定的 ACP ID 与持久化按需 ACP-to-SQLite-ID alias、session create/list/load/resume/fork/delete/prompt/cancel/close、有界/脱敏回放、失败关闭权限、session-owned MCP 工具、有界额外目录、由能力协商控制的客户端文本读取/精确替换、由能力协商控制的客户端终端，以及有界内嵌图片和内嵌文本资源提示。声明 `loadSession: true`、list/delete/fork/resume/close，以及 MCP `http: true`/`sse: true` capability。客户端声明时，`read_file` 会委托给 `fs/read_text_file`；只有同时声明文本读写能力才暴露 `search_replace`，并保留 sandbox、工作区、指令和权限门禁。当声明 `terminal: true` 且 sandbox 仅为 `off` 时，`terminal_exec` 会通过 create/wait/output/release 发送一个直接可执行文件和参数；`terminal_start`/`terminal_output`/`terminal_wait`/`terminal_kill` 增加由 session 持有的标准后台生命周期，使用不透明 task ID、最多八个运行中任务和 32 个保留任务，并在超时或清理时 kill/release。它们不会转发已配置环境、保留输出上限为 1 MiB，也没有本地回退。Image block 会按光栅 MIME 白名单校验 base64，在持久 session 中保留有序的原生供应商上下文，并在 ACP 历史中只投影安全占位符；其 URI、字节与 URL 都不会回放。内嵌 `TextResourceContents` 只接受已提供文本和不可解析的来源标签，会在持久历史中保留有界、带标签的文本并省略 `_meta`/annotations；内嵌 `BlobResourceContents` 仍会被拒绝。客户端载荷错误会失败关闭且不携带原始详情。创建/加载/恢复/分叉接受有界 stdio、Streamable HTTP 和 legacy SSE server；ACP 传输 server 与非工具 MCP 能力仍未支持。启用的 sandbox 仍拒绝额外目录，因为其挂载已固定。客户端交互式终端输入/resize/PTY framing、音频、内嵌二进制 prompt、二进制多媒体历史回放和 WebSocket 仍待实现；私有 `_neuro-code/session/artifacts` 扩展已实现会话作用域的有界 artifact 列出/读取，标准扩展面仍未支持。 |
| MCP 服务器 | M4 | partial | 已集成官方 `mcp>=1.28.1,<2` 的 `ClientSession`、Schema、协商、JSON-RPC 调度、工具枚举与调用，用于有界 ACP stdio、Streamable HTTP 与 legacy SSE server。stdio 保留换行桥接、POSIX 进程组与 Windows 原子 Job 保证。远程 endpoint 必须使用不含内嵌凭据或 fragment 的已校验 HTTP/HTTPS URL、受限且非保留 header，保持 TLS 校验、不继承环境代理或重定向，并限制 1 MiB 响应体。所有传输都实施上限、受保护环境拒绝、显式值脱敏、`_meta` 省略、冲突检查、仅元数据 ResourceLink、ASK 高于 bypass 审批与幂等清理。远程取消会关闭且不再复用本地连接；ACP 传输、resources、prompts、sampling、elicitation、动态 list 刷新、配置持久化和多媒体/embedded 投影仍待实现。 |
| 技能与 AGENTS.md | M4 | partial | 已实现精确文件名 `AGENTS.md` 的有界继承发现，以带标记的临时 User 上下文注入，并在每个模型步骤前按绑定内目标刷新；`search_replace` 会对比最近一次实际注入的路径/内容快照，遇到模型未见的变更即中止。只读技能发现覆盖嵌套 LOCAL、从工作区上方到 git 根的每个 REPO 祖先以及 USER 主目录，采用确定性的 `LOCAL > REPO > USER` 遮蔽、遍历/条目/文件/字节/目录硬上限、拒绝所有链接/reparse point，并使用正文敏感指纹。有界 `skill` 工具会校验发现指纹、剥离 frontmatter、安全采样捆绑文件名，并支持有界的 `$ARGUMENTS`、位置参数及 `${SKILL_DIR}` 替换。其他指令文件名/rules、`.cursor` 与供应商默认兼容、条件技能、远程/服务器/内置技能、hooks/plugins 和子代理集成仍待实现 |
| 钩子与插件 | M4 | planned | 钩子和插件市场 crate |
| 子代理与计划模式 | M4 | partial | 已通过 `update_plan` 实现模型管理的持久会话结构化计划；它可跨恢复保留、在分叉时继承，并可在 TUI 中查看。本地 `/comment-plan STEP COMMENT` 会向当前计划修订最多添加 48 条有界评论，在 `/view-plan` 中显示，并在不产生批准或执行效果的前提下提供给下一次供应商请求。经用户确认的 `/execute-plan`/`/run-plan` 交接会创建具有明确终态且保存被交接计划不可变快照的持久不透明执行任务记录；`/tasks` 只显示短指纹和已完成步骤数，而显式 `/view-task TASK_ID` 只会从当前会话读取完整已保存快照并作为不可执行的参考展示，随后只切换到 `accept-edits`；已实现有界显式 `/schedule-plan`/`/queue-plan` 和 `/run-task TASK_ID`。Stage5CQ 新增显式应用层 `SubagentExecutionService`，Stage5CR 新增具体的全新会话只读运行时、持久父子链接、固定 `read_file`/`list_dir`/`grep`/`skill` 能力集以及有界步数/时间和取消清理，Stage5CS 新增调用方使用的脱敏有界结果投影且不写入父 transcript，Stage5CT 新增只读父子关系投影和仅表示能力的生命周期标签，Stage5CU 新增明确的 `neuro subagent --parent-session SESSION_ID PROMPT` CLI 入口以及有界普通/JSON 输出，Stage5CV 新增私有 `_neuro-code/session/subagent` ACP 扩展以及有界响应/状态/outcome 输出，Stage5CW 新增使用相同有界只读服务的 TUI `/subagent PROMPT` 显式入口，Stage5CX 新增基于安全关系投影的显式 TUI `/subagents` 只读视图，Stage5CY 新增由父关系和终态校验保护的显式 `/subagents resume|fork|delete TASK_ID` 控制。当前仍不复用父上下文，也没有自动调度、重试、递归创建、并行子会话或可写工具 |
| 显式子代理生命周期 CLI 控制 | M4 | partial | Stage5CZ 新增 `neuro subagents ACTION TASK_ID --parent-session SESSION_ID`，通过 application 生命周期 owner 执行显式 `resume`、`fork` 和 `delete`。普通和 typed JSON 投影只包含有界生命周期标识；命令不运行模型、不重放工具、不自动打开 fork，也不直接读取 SQLite。 |
| 显式 ACP 子代理生命周期扩展 | M4 | partial | Stage5DA 新增私有 `_neuro-code/session/subagents`，请求严格包含外部 `sessionId`、有界 `taskId` 以及 `resume`/`fork`/`delete` 动作。Resume/fork 返回外部 ACP alias，delete 只返回 deleted 标志；模型回合、工具重放、递归/并行子会话、可写工具、内部 ID 和子会话正文不会跨 wire。 |
| 失败关闭的 ACP 子代理生命周期投影 | M4 | partial | Stage5DB 校验返回的父 session/task/action 关联，并在 wire 序列化前限制非 delete 外部 alias。错误的注入结果会失败关闭；有效生命周期响应及 Runtime/会话行为保持不变。 |
| ACP 子代理 alias 重连兼容性 | M4 | partial | Stage5DC 将生命周期 alias 分配限制为四次，在序列化前把每个 alias 解析回预期子会话，针对冲突或错误所有者结果重试，并在重复 resume 请求和 ACP 客户端重连后保留持久 alias。私有扩展、子代理执行、schema 和 ACP 标准 capability 集保持不变。 |
| 确定性上下文压缩评估 | M5 | partial | Stage5DD 新增 canonical `neuro_code.application.memory.compaction` planner，提供类型化用量、软/硬阈值、受保护前缀和近期后缀边界以及仅包含索引的有界计划。未知容量明确返回 `UNAVAILABLE`；不生成摘要、不调用 Provider、不创建持久条目、不接入 Runtime，也不改变会话/UI/wire 行为。 |
| Provider 感知的上下文摘要请求边界 | M5 | partial | Stage5DE 在 canonical memory 模块新增有界 `ProviderContextWindow` 元数据与 `ContextSummaryRequest` 投影。已知容量会裁剪摘要预算；未知容量、不可执行计划和空候选区间失败关闭。不增加 Provider 参数、不调用模型、不创建提示词或持久条目，也不接入 Runtime 或改变接口行为。 |
| Provider 感知的脱敏摘要输入 | M5 | partial | Stage5DF 新增 `ContextSummaryInputBuilder` 与类型化的有界投影。它会在控制字符/UTF-8 限制前处理显式值和常见凭据形状，省略工具参数、推理和保留的 Provider 载荷，并使用注入的本地 token 估算器受 Provider 窗口剩余预算约束。不调用 Provider、不构建提示词、不修改上下文、不持久化压缩条目，也不改变 Runtime/接口行为。 |
| Provider 驱动的有界上下文摘要生成 | M5 | partial | Stage5DH 新增 `ProviderContextSummaryGenerator` 和 `ContextSummaryGenerationResult`。它只接受脱敏且有界的输入，校验 Provider/model 亲和性，构建临时提示词，恰好使用 `DISABLED` 无工具请求，缓冲增量并优先使用 `ModelCompleted.response_text`，再次脱敏/限制结果；缺少/空/多个完成事件或远端工具事件会失败关闭。不启用自动压缩、重试、持久化、事件或整轮原子性。 |
| 记忆与上下文压缩 | M5 | partial | Stage5DD 建立确定性评估；Stage5DE/DF 增加 Provider 感知的有界请求/输入契约；Stage5DG 增加持久化压缩条目和显式恢复重建；Stage5DH 增加单请求严格无工具摘要生成；Stage5DI 增加带调用前源指纹校验并委托 `SessionStore` 的显式应用持久化服务；Stage5DJ 固定兼容边界：压缩行不进入导入/导出和分叉复制，`finalize_turn` 只对自身的事件/条目/搜索/执行记录事务提供原子性。Runtime 自动触发和有界事件仅在 ADR 0107 的受控、已持久化且 Provider 容量已知条件下启用；Provider 生成与压缩持久化仍是分离的事务。 |
| 显式上下文压缩触发 | M5 | partial | Stage5DK 新增默认关闭的 `ContextCompactionTriggerService` 应用层边界。`DISABLED` 只进行纯评估；`EXPLICIT` 只有在调用方提供过期源元数据后才委托可执行计划。触发服务不改变 Runtime、Provider、SessionStore schema、导入/导出、事件、CLI、TUI 或 ACP 行为，也不复用普通回合预算。见 [ADR 0091](adr/0091-explicit-context-compaction-trigger.md)。 |
| Runtime 压缩安全边界门控 | M5 | partial | Stage5DL 新增带 `BEFORE_MODEL_REQUEST`/`AFTER_TOOL_BATCH` 安全位置的类型化门控、活动操作/取消失败关闭检查以及独立的单请求/无工具预算契约。不接入 `AgentRuntime`、不新增事件、不启用自动触发、不实现超时，也不宣称整轮事务原子性。见 [ADR 0092](adr/0092-runtime-compaction-safe-boundary.md)。 |
| 强制执行 Runtime 压缩超时 | M5 | partial | Stage5DM 新增有限且真正执行的墙钟限制（默认 30 秒，最大 300 秒），覆盖允许的显式压缩摘要生成和持久化调用。超时抛出 `ContextCompactionTimeoutError`；Provider 错误、存储错误和取消保持不变。不接入 `AgentRuntime`、不启用自动压缩，也不宣称跨操作事务原子性。见 [ADR 0093](adr/0093-enforced-context-compaction-timeout.md)。 |
| Runtime 压缩失败投影 | M5 | partial | Stage5DN 新增 `classify_context_compaction_failure()`，将超时有界投影为可恢复的 `BUDGET_LIMITED/WALL_TIME_BUDGET`（`finalized=false`），并指定 `TURN_FINALIZATION` 记录策略。取消、Provider 和存储失败仍只传播；未知异常保持未分类。投影不保存错误详情，不修改 `AgentRuntime`、事件或自动压缩，也不声称跨操作事务原子性。见 [ADR 0094](adr/0094-runtime-compaction-failure-projection.md)。 |
| 显式 Runtime 压缩接缝 | M5 | partial | Stage5DO 新增可选且默认关闭的 `AgentRuntime.compaction_runtime_gate` 与 `trigger_context_compaction()`，接收调用方完整提供的安全边界请求。缺少配置时失败关闭；委托不会修改上下文、普通回合 steps、事件或执行记录。`AgentRuntime.run()`、Composition、自动触发和生产 gate 组装保持不变。见 [ADR 0095](adr/0095-explicit-runtime-compaction-seam.md)。 |
| 由应用层拥有的显式压缩调用方 | M5 | partial | Stage5DP 为每个 `ApplicationComposition` binding 组装带脱敏边界的 `ContextCompactionRuntimeGate` 并注入 `AgentRuntime`，但不启用自动触发。`AgentConversation.trigger_context_compaction()` 是显式调用方，使用现有会话回合锁串行化压缩，对 `EXPLICIT` 请求要求匹配的持久化会话，并委托调用方拥有的不可变快照。transcript、事件、普通回合预算和 `finalize_turn()` 事务性保持不变。见 [ADR 0096](adr/0096-application-owned-compaction-caller.md)。 |
| 带压缩的回合最终化原子事务 | M5 | partial | Stage5DQ 增加可选的 `SessionStore.finalize_turn_with_compaction()`；SQLite 原子提交完成事件、会话条目/搜索投影、可选执行记录和一个持久化压缩行。普通 `finalize_turn()` 与 `save_compaction_item()` 保持不变，Provider 生成不在事务内，当前还没有 Runtime 或自动压缩调用方使用该边界。详见 [ADR 0097](../en/adr/0097-atomic-turn-finalization-with-compaction.md)。 |
| 回合记录器压缩最终化所有者 | M5 | partial | Stage5DR 允许 `TurnEventRecorder.finalize_turn_completion()` 使用预先校验的 `DurableCompactionItem` 选择原子存储方法；普通调用保持不变，非法输入会在内存完成事件加入前失败，当前 Agent loop 和自动触发器都不会提供该条目。Provider 生成和失败投影仍由调用方拥有。详见 [ADR 0098](../en/adr/0098-turn-recorder-compaction-finalization-owner.md)。 |
| 类型化压缩回合投影 | M5 | partial | Stage5DS 新增 `ContextCompactionTurnProjection`：成功触发只转移已持久化且校验过的条目；超时携带有界可恢复预算 outcome；取消、Provider、存储和未知失败不会被静默消费。不启用事件、持久化、自动压缩或普通 loop 行为。详见 [ADR 0099](../en/adr/0099-context-compaction-turn-projection.md)。 |
| 显式压缩回合所有者 | M5 | partial | Stage5DT 新增可选的 `TurnEventRecorder.finalize_turn_from_compaction_projection()` 接缝。成功投影要求调用方提供普通 outcome 并使用回合加条目的原子存储；超时只消费自身有界 outcome；无操作和只能传播的投影失败关闭。普通 loop 和自动压缩保持不变。详见 [ADR 0100](../en/adr/0100-explicit-compaction-turn-owner.md)。 |
| 在回合锁下由应用层拥有压缩 | M5 | partial | Stage5DU 新增 `AgentConversation.run_context_compaction_with_owner()` 及对应的 `ConversationRunner` 协议方法。它校验调用方拥有的不可变请求，在现有会话回合锁下串行化门控与所有者，只消费成功或受控超时投影，并在调用所有者前对无操作投影失败关闭。取消、Provider、存储和未知失败保持传播；普通 loop、自动压缩、事件和 UI 保持不变。详见 [ADR 0101](../en/adr/0101-application-compaction-owner-under-turn-lock.md)。 |
| 上下文用量快照与过期源请求构造 | M5 | partial | Stage5DV 新增无副作用的应用层辅助入口：有 Provider 输入/输出用量时使用报告值，否则使用有界 `ModelContext` 估算；未知 Provider 容量保持未知；仅对可执行的显式请求计算过期源指纹。它们不调用 Provider/存储、不修改上下文，也不启用自动压缩。详见 [ADR 0102](../en/adr/0102-context-usage-snapshot-and-stale-source-builder.md)。 |
| 显式实时上下文压缩命令 | M5 | partial | Stage5DW 新增 `AgentConversation.run_explicit_context_compaction_with_owner()`。它在现有会话锁内构建带实时指引的上下文，委托已配置 Runtime gate 构造 usage/过期源保护，并生成有界身份/时间元数据后复用 typed owner 投影。要求已有持久化会话，仍是显式操作：不接入普通 loop、不自动触发、不发事件、不修改 transcript，也不把 Provider 与存储放入同一事务。详见 [ADR 0103](../en/adr/0103-explicit-live-context-compaction-command.md)。 |
| 显式压缩命令投影 | M5 | partial | Stage5DX 新增 `ContextCompactionCommandResult` 以及有界 CLI/ACP 序列化器。`completed`、`not_needed` 和受控 `budget_limited` 超时结果只暴露不透明 ID、计数和摘要 token 元数据；Provider、取消、存储和未知失败仍然是异常。不启用命令、事件、普通 loop 接入或自动触发。详见 [ADR 0104](../en/adr/0104-explicit-compaction-command-projection.md)。 |
| 统一普通执行预算与 REPLAN 指引 | M3/M5 | partial | 正式 CLI/TUI/ACP 入口默认使用应用层拥有的 `normal` 48/48/192 模型/工具轮次/工具调用档位，`deep` 使用 96/96/384。旧 `--max-steps N` 现在把全部计数型普通上限同步映射为 N/N/4N；Finalizer 尝试继续独立。在受控模式下，批次结束的 `REPLAN` 决策会注入不持久化的合成指引直到产生新进展，每次请求还会收到与 Provider 无关的 batch-first 指引。现有顺序工具执行和 stuck 检测保持不变。详见 [ADR 0105](../en/adr/0105-unified-execution-budget-and-replan-guidance.md)。 |
| 有界长任务 Runtime | M3/M5 | partial | 受控回合现在会向模型和有界接口事件投影类型化预算压力；当会话已持久化且 Provider 上下文窗口已知时，在安全边界自动压缩；并在不重置全局执行预算的前提下发出有界进展分段检查点。标准 TUI 有意不展示原始执行计数。合成预算/检查点指引和压缩请求不进入规范会话。压缩 Provider 生成与持久化是分离操作，分段检查点是审计标记而非崩溃恢复或工作区回滚点。仍未启用自动委派。详见 [ADR 0107](../en/adr/0107-bounded-long-task-runtime.md)。 |
| LSP、工作树与检查点 | M5 | planned | 工具/工作区/worktree crate |
| 语音、图像、视频和网页工具 | M5 | planned | 需要由提供商支持的适配器 |
| Leader、relay 与 Computer Hub | M5 | planned | 本地组件与外部服务边界 |

## ACP 私有扩展

私有命名空间扩展 `_neuro-code/session/artifacts` 已实现,可有界地列出/读取与会话关联的本地脱敏工具输出
artifact.它不会作为标准 ACP capability 宣告;其他扩展仍不支持.
