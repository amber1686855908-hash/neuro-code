# Compatibility matrix

[简体中文](../zh-CN/compatibility-matrix.md) · **English**

Statuses: `unassessed`, `planned`, `partial`, `compatible`, or
`intentionally-different`. “Evidence” points into the pinned Rust repository.

| Capability | Target | Status | Evidence / notes |
|---|---|---|---|
| Package and CLI composition root | M1 | partial | The normal package includes Textual and publishes equivalent `neuro` and `neuro-code` console scripts; `neuro code` is the explicit TUI alias, all three use the launch directory as the workspace, and globally installed tools require no per-use venv activation. CLI, TUI, and ACP share an interface-neutral `ApplicationComposition`; standalone native installers remain pending |
| `version` and JSON version output | M1 | partial | Independent package metadata implemented |
| Effective configuration inspection | M1 | partial | `neuro-code inspect`; secret-redacted Python view implemented |
| Named provider profiles and selection | M2/M3 | partial | Four explicit wire protocols, default/one-shot selection, legacy TOML compatibility, redacted `providers list/inspect`, first-run TUI setup, a category-first Settings flow, multi-profile create/edit/save-and-use/confirmed deletion, explicit OpenAI Responses/compatible Chat/DeepSeek presets, user-triggered connection testing with a bounded in-memory remote model picker and manual fallback, and a picker with persisted managed defaults through `/provider`, `/model`, and Ctrl+P are implemented. The generic Responses adapter is implemented only at `neuro_code.providers.openai_responses.OpenAIResponsesProvider`; xAI remains an `openai-responses` dialect, and the development-stage cleanup removed `neuro_code.providers.xai_responses` plus `XAIResponsesProvider`. Metadata and API keys use separate atomic private files behind a port; stored values are representation-safe and redacted at tool-result boundaries. The managed JSON reader is public only at `neuro_code.configuration.managed_provider_settings`, `JsonProviderSettingsStore` remains the adapter API, and `ProviderProfile`/`AppConfig` remain the config types after removal of loader re-exports and the `ProviderConfig` alias. Same-name managed profiles replace whole TOML tables to prevent workspace endpoint redirection; deletion removes profile metadata plus its credential entry and safely selects a remaining default. OS-keychain storage and durable/offline model-catalog caching remain pending |
| CC Switch compatibility | M2 | partial | Read-only `NEURO_CODE_CC_SWITCH_CONFIG` mapping, three backend formats, loopback `PROXY_MANAGED`, env references, inline-key rejection, and native project override implemented; CC Switch database/process control and its internal failover remain excluded |
| Safe provider failover | M2 | partial | Ordered lazy fallback profiles, first-event commit boundary, monotonic per-run selection, audited failures/selections, `--no-failover`, aggregate errors, and opaque-session origin protection implemented; retry, circuit breaking, and persistent health remain pending |
| Provider HTTP proxy policy | M2/M3 | partial | Per-profile environment/direct/explicit-env modes, TUI editing and pre-save validation, in-TUI recovery from a failing managed default during startup, uniform four-adapter HTTPX options, redacted inspection/errors, strict ambiguous-SOCKS diagnostics, and a `socks` installation extra are implemented. Explicit routing was manually verified against DeepSeek while an invalid inherited `ALL_PROXY` remained present; PAC, multi-proxy mounts, and default-bundled SOCKS support remain pending |
| Headless single prompt | M2 | partial | Streaming text/tool loop, plain/JSON/JSONL output implemented |
| Application-owned reasoning effort | M3 | partial | Provider-neutral `low`/`medium`/`high`/`xhigh`/`ultracode`, default `high`, request-only review guidance, typed `ModelContext` propagation, `--effort`, TUI Ctrl+E and `/effort`/`/reasoning`, requested/effective display, and atomic user-preference persistence implemented. Current adapters do not send provider-private effort parameters; `ultracode` explicitly falls back to `xhigh` and starts no sub-agent workflow. Capability-gated native mappings and workflow/sub-agent orchestration remain pending |
| Interactive operating modes | M3 | partial | Application-owned `normal`/`accept-edits`/`plan`/`auto`, `Shift+Tab` cycling, `/mode MODE` completion, runtime display, atomic preference persistence, profile/session reapplication, request-only model guidance, and deterministic permission-mode mappings implemented. `auto` is a labelled safe preview using accept-edits defaults unless startup explicitly authorized `--always-approve`; a model safety classifier remains pending, while explicit rules and process sandboxes always remain active |
| Turn and tool timing feedback | M3 | partial | Monotonic time-to-first-actionable-result events per model step, whole-turn completion/failure duration, local/hosted tool duration, one call-ID-stable card per tool, concise one-line read feedback with expandable bounded results, permission-path feedback, automatically expanded edit slices with green/red tinted diffs, a final elapsed line after the assistant response, and in-place click/keyboard detail collapse/expansion implemented. Durations are client observations rather than provider-private reasoning telemetry; durable full tool transcripts remain pending |
| Context-window budget and usage display | M3 | partial | Optional positive per-profile `context_window_tokens`, redacted selection/failover propagation, startup/resume estimation, provider-reported input-plus-output usage events, localized permanent percentage/tooltip, `/status` reporting, and 50%/80% warning colors implemented. The field is local metadata and is not sent as an API parameter; exact preflight provider tokenization and automatic context compaction remain pending |
| OpenAI-compatible/xAI Chat endpoint | M2 | partial | Chat Completions streaming, bounded `max_tokens`, tools, validated native user-image blocks, thinking-mode tool-roundtrip continuity, and fail-closed upstream-import visible-context replay implemented; double-gated DeepSeek live streaming/failover/read-tool tests implemented and manually passed, while recorded model-specific fixtures remain pending |
| OpenAI Responses/xAI dialect | M2 | partial | Generic portable Responses subset plus optional xAI encrypted reasoning, web/X/code hosted tools, profile-affine native replay, strict legacy official-host fallback, terminal-authoritative output, SSE lifecycle normalization, status stripping, fallback repair, and error redaction implemented; advanced hosted-tool filters, stateful response IDs, compaction items, and opt-in live fixtures remain pending |
| Anthropic provider | M2 | partial | Native Messages SSE, text/reasoning, tools, usage, errors, secret redaction, and validated user/tool-result image blocks implemented; live/model-specific fixtures pending |
| Gemini provider | M2 | partial | Native `streamGenerateContent` SSE, text/thoughts, tools, usage, errors, thought-signature round trip, and validated user inline/File API image parts implemented; live/model-specific fixtures pending |
| Core tool registry and schemas | M2 | partial | Independent Python contracts plus capability-gated background Bash/task-output/wait-tasks/kill schemas implemented |
| Read/list/grep tools | M2 | partial | Workspace-contained UTF-8 baseline implemented |
| Search/replace editing | M2 | partial | Atomic exact replacement and containment implemented |
| Bash execution and cancellation | M2/M3 | partial | Bounded foreground output, null stdin, configured provider/proxy credential stripping, timeout/cancel cleanup, POSIX process-group TERM/KILL, fail-closed Windows Job Object creation with atomic `PROC_THREAD_ATTRIBUTE_JOB_LIST` assignment/restricted standard-handle inheritance/descendant wait/whole-Job termination, conversation-scoped managed background start/snapshot/bounded single- and multi-task event waits/timeout/idempotent-kill/binding-switch/application-shutdown lifecycle, and bounded model-only completion reminders with task-output/wait-tasks/kill deduplication implemented; explicit reviewed secret injection, full-output files, and model auto-wake/auto-background remain pending |
| Permission rule precedence | M2/M3 | partial | Deny/ask/allow precedence, per-segment `&&`/`||`/`;`/pipe checks, wrapper peeling, nested `bash -c`, fail-closed complex scripts, asynchronous allow-once/deny, memory-only exact-action session approval, and per-ACP-session `session/request_permission` adaptation implemented; local deny/workspace/sandbox decisions remain authoritative and pending tool updates precede client approval. Approval request/response contracts are available only from `neuro_code.application.permissions.contracts`; development-stage cleanup removed their root `neuro_code.permissions` re-exports, while that root module retains permission policy. Persistent reviewed rules and full rule/file-access grammar remain pending |
| SQLite session event store | M2/M3 | partial | Schema v5, atomic and concurrent-initializer-serialized v1/v2/v3/v4 migrations, profile-affinity and fixed-sandbox metadata, stable titles, synchronized FTS5 documents, namespaced one-to-one external-session aliases, immutable read-only resume preflight, canonical ordered session-item persistence, message projection, append-only prefix checks, list, resume, export, and filesystem-identity workspace matching across platform path aliases implemented |
| Session titles and full-text search | M3 | partial | Deterministic ten-word titles from the first visible user prompt, upstream `generated_title` preservation, atomic manual title/FTS updates through `sessions rename` and TUI `/rename`/`/title`, title-weighted SQLite FTS5, multi-token AND-to-OR fallback, cwd filtering, pagination, matched fields, optional snippets, `sessions search`, and workspace-scoped TUI `/sessions QUERY` results implemented; system messages, private reasoning/native items, tool arguments/raw results, and image URLs are not indexed. Model-generated titles, a live debounced search field, and the ACP method remain pending |
| Rust session import | M2 | partial | Read-only v0/v1 parsing, mixed legacy/current messages, structured images, ordered reasoning/backend-tool payloads, embedded `raw_output`/singular-reasoning recovery with backend-ID deduplication, bounded corrupt-line recovery, atomic SQLite import, built-in sandbox-profile and generated-title preservation with custom-profile rejection, prefix-safe resume, export v4, provider-native image replay, trusted-source xAI Chat replay, and strict-affinity native Responses encrypted/backend-tool replay implemented; compaction and stateful IDs remain pending |
| Full-screen/minimal TUI | M3 | partial | Textual prompt, parameter-aware slash-command hints with inline suggestion and Tab-first completion, distinct full-width user and assistant blocks, stable in-place streamed responses with scroll-follow protection, safe assistant Markdown with cool semantic colors, aligned system/status/error gutters, one-label in-place tool cards with bounded redacted output/file diffs and click/keyboard detail toggling, highlighted values, structured timing, a persistent localized provider/model/workspace/context-usage/effort/mode bar, five-level Ctrl+E and `/effort`/`/reasoning` selection, four-mode Shift+Tab and `/mode` selection, persistent English/Simplified-Chinese application chrome through Ctrl+, and `/settings`/`/setting`, bounded user-triggered provider connection/model discovery, missing-resize-event viewport reconciliation, plain-text search with the conflicting emoji command palette disabled, durable multi-turn context, fail-closed approvals, safe configured-profile/session selection and replay/search/rename, read-only scoped `/tasks` visibility, local lifecycle commands, Ctrl+C in-flight cancellation, same-session retry, balanced cancellation results, Textual return-code propagation, supervisor cleanup in `finally`, cross-platform headless lifecycle tests, and opt-in production-CLI tests that send real `Ctrl+Q` through a POSIX PTY or Windows ConPTY, verify paired terminal teardown, compare full POSIX `termios`, and exercise Windows idle `Ctrl+C`, resize, non-zero exit and available parent console modes; a live debounced search field, pristine pre-token rewind/interjection queues, and model completion auto-wake remain pending, so M3 is not complete |
| Markdown/Mermaid/media rendering | M3/M5 | partial | Safe assistant Markdown uses an application-owned semantic theme, does not interpret model text as Rich/Textual markup, and disables hyperlink activation; headings, emphasis, code, lists, links, and tables are rendered. Tool output and diffs use literal application-styled text. Mermaid and inline media remain pending |
| PTY and process-tree parity | M3/M4 | partial | Foreground/background commands share an owned `ProcessTree`; POSIX waits for a surviving process group, while Windows creates the leader atomically inside a kill-on-close Job Object, restricts inherited standard handles, waits for descendants after the leader exits, and uses handle-based whole-tree termination. A shared interactive-terminal domain/port and manager now provide bounded cursor-addressed tail output, explicit truncation, input/resize/signal/wait/close, permission/workspace/sandbox gating, cancellation-safe creation and registry shutdown. POSIX sessions own a PTY process group; production Windows sessions atomically combine ConPTY and Job-list attributes and terminate the whole Job. Portable attribute/handle/error tests, real Linux PTY tests, native process-tree tests, and opt-in three-platform CLI terminal smoke cover the boundary. The partial ACP core still does not call client `terminal/*` or expose interactive PTY framing/backpressure |
| OS sandbox profiles | M3 | partial | Canonical `off`/`workspace`/`read-only`/`strict` configuration and CLI override, user-profile protection from project weakening, Linux bubblewrap whole-process filesystem enforcement, mount attestation, read-only edit-schema/runtime gates, child-only network namespaces, trusted-helper checks, fail-closed unsupported platforms, and session-fixed profile persistence/resume conflict detection implemented; custom profiles, `devbox`, deny globs, Landlock/Seatbelt, and Windows enforcement remain pending |
| ACP stdio/WebSocket | M4 | partial | Official `agent-client-protocol==0.11.0` stdio SDK implements protocol-version negotiation, saved client capabilities/info, stable workspace-bound ACP IDs with durable lazy ACP-to-SQLite-ID aliases, `session/new`, workspace-scoped bounded cursor `session/list`, `session/load`, ordered bounded Text/ResourceLink prompt conversion, bounded/redacted visible-history replay, safe resume configuration, streamed text/tool/usage updates, fail-closed permission requests, one active prompt per session with cross-session concurrency, silent cancel notifications, close without history deletion, creation/EOF/disconnect cleanup, and session-owned stdio MCP tool servers. `loadSession: true` and `sessionCapabilities.list = {}` / `close = {}` are advertised; no optional MCP HTTP/SSE capability is declared because only baseline stdio is implemented. List omits content/private/provider metadata and uses random connection-local cursors that do not expose internal IDs. Non-empty `additionalDirectories` remain rejected. Resume/delete/fork, MCP HTTP/SSE/ACP transports and non-tool MCP features, image/audio/embedded content and multimedia replay, client `fs/*`/`terminal/*`, WebSocket, and extensions remain unsupported. SDK 0.11 requires its unstable-router switch to expose `session/close`, ignores malformed JSON frames, and accepts a wrong incoming `jsonrpc` version before emitting a normalized 2.0 response; no custom production ACP dispatcher masks those SDK behaviors |
| MCP servers | M4 | partial | Official `mcp>=1.28.1,<2` `ClientSession`, schemas, negotiation, JSON-RPC dispatch, tool listing and calls are integrated for bounded ACP-provided stdio servers. A project-owned newline bridge preserves the existing POSIX process-group and atomic Windows Job ownership guarantees. Server/config/tool/frame/schema/argument/result limits, protected environment rejection, explicit-value redaction, `_meta` omission, collision checks, metadata-only ResourceLinks, stderr isolation, exact ASK-over-bypass permission rules, cancellation-before-response process-tree termination, and idempotent session/creation/disconnect cleanup are implemented. HTTP/SSE/ACP transports, resources, prompts, sampling, elicitation, dynamic list-change refresh, persistence of MCP configuration, and multimedia/embedded result projection remain pending |
| Skills and AGENTS.md | M4 | partial | Bounded exact-name `AGENTS.md` inheritance is injected as transient tagged User context, refreshed from a binding-local target before each model step; `search_replace` compares against the last injected path/content snapshot and aborts on unseen changes. Read-only skill discovery scans nested LOCAL roots, every REPO ancestor through the git root, and USER home with deterministic `LOCAL > REPO > USER` shadowing, hard traversal/entry/file/byte/catalog limits, all links/reparse points rejected, and body-sensitive fingerprints. The bounded `skill` tool validates the discovery fingerprint, strips frontmatter, safely samples bundled file names, and supports bounded `$ARGUMENTS`, indexed arguments, and `${SKILL_DIR}` substitution. Alternate instruction names/rules, `.cursor` and vendor-default compatibility, conditional skills, remote/server/bundled skills, hooks/plugins, and subagent integration remain pending |
| Hooks and plugins | M4 | planned | Hooks and plugin marketplace crates |
| Subagents and plan mode | M4 | planned | Lifecycle/session/goal modules |
| Memory and compaction | M5 | planned | Memory and compaction crates |
| LSP, worktree, checkpoints | M5 | planned | Tools/workspace/worktree crates |
| Voice, image, video, web tools | M5 | planned | Provider-backed adapters required |
| Leader, relay, Computer Hub | M5 | planned | Local pieces plus external service boundary |
