# Compatibility matrix

[简体中文](../zh-CN/compatibility-matrix.md) · **English**

Statuses: `unassessed`, `planned`, `partial`, `compatible`, or
`intentionally-different`. “Evidence” points into the pinned Rust repository.

The current production Windows sandbox source verification is CI run
`32374860136` at head `1175fa046cde263a9a1c9afbb1a32b7f6ed34914`; it completed
all 23 jobs. The final branch update is evidence-only documentation.

| Capability | Target | Status | Evidence / notes |
|---|---|---|---|
| Package and CLI composition root | M1 | partial | The normal package includes Textual and publishes equivalent `neuro` and `neuro-code` console scripts; `neuro code` is the explicit TUI alias, all three use the launch directory as the workspace, and globally installed tools require no per-use venv activation. CLI, TUI, and ACP share an interface-neutral `ApplicationComposition`; standalone native installers remain pending |
| `version` and JSON version output | M1 | partial | Independent package metadata implemented |
| Effective configuration inspection | M1 | partial | `neuro-code inspect`; secret-redacted Python view implemented |
| Provider service catalog and preset ownership | M3/P3A/P3B | partial | `ProviderServiceDescriptor` / `ProviderServiceCatalog` describe the thirteen current services, including Ark, Qianfan, Bailian, and TokenHub, preserve existing UI keys and profile matching, and are consumed by an injectable TUI catalog. Service metadata does not own credentials or runtime execution; adding a future service is descriptor-plus-tests work. P3B keeps service, publisher, protocol, model, endpoint, and billing/workspace metadata separate. See [ADR 0117](adr/0117-provider-service-capability-routing-foundation.md), [ADR 0122](adr/0122-china-direct-provider-compatibility-expansion.md), and [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| Canonical model and hosted-tool capabilities | M3/P3A/P3B | partial | `ModelCapabilitySet` provides supported/unsupported/unknown status. Runtime preserves upstream service → protocol → model facts, meets them with trusted adapter implementation capabilities, and applies only explicit configuration restrictions; metadata cannot elevate an unsupported or unknown adapter. P3B adds model-specific Chat/Responses/Anthropic matrices, optional publisher hints, and TokenHub native/compatibility-converted/unsupported Responses metadata without adding hosted vendor tools. Existing xAI builtin tools remain unchanged; standard OpenAI Responses exposes hosted Web Search only when `builtin_tools = ["web_search"]` is explicit. Gemini Interactions adds protocol/model-specific Google Search, URL Context, and `MIXED_HOSTED_AND_CLIENT_TOOLS` facts; unknown Gemini models remain fail-closed. Kimi, GLM, MiniMax, Ark, Qianfan, Bailian, and TokenHub hosted web status remains unknown or unsupported unless a separate trusted implementation exists. See [ADR 0117](adr/0117-provider-service-capability-routing-foundation.md), [ADR 0118](adr/0118-hosted-web-search-execution-and-sidecar-routing.md), [ADR 0120](adr/0120-gemini-interactions-and-hosted-web-tools.md), [ADR 0122](adr/0122-china-direct-provider-compatibility-expansion.md), and [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| Runtime roles and model routes | M3/M5 | partial | `RuntimeRole.MAIN` and optional `WEB_SEARCH` routes are validated, redacted, and executed independently; legacy `[routing] default`/`fallbacks` project to MAIN while role-specific fallbacks remain isolated. `WebSearchMode` resolves disabled/auto/inline/sidecar fail-closed, while `WebFetchMode` resolves disabled/auto/local/inline without a sidecar: hosted fetch is retained only for explicit MAIN support and local fetch is registered only after the selected mode permits it. See [ADR 0118](adr/0118-hosted-web-search-execution-and-sidecar-routing.md) and [ADR 0121](adr/0121-local-safe-web-fetch.md) |
| Hosted Web Search and Web Fetch vertical slice | P1/P1.2/P2/P3A | partial | Canonical bounded request/source/citation/result contracts, `WebSearchService`, independent WEB_SEARCH fallback, OpenAI standard Responses and xAI sidecars, Anthropic Messages `web_search_20260318`/`web_fetch_20260318` server tools, and Gemini Interactions Google Search/URL Context hosted capabilities are implemented. Gemini uses stable stateless Interactions requests, typed step lifecycle, bounded native replay, structured `url_citation` extraction, forced Google Search sidecars, and capability-aware inline versus sidecar routing; DeepSeek/OpenAI-compatible MAIN → local `web_search` → search route → canonical `ToolResult` remains intact. Kimi, GLM, and MiniMax MAIN profiles use that same local sidecar path and P2 local fetch; no native hosted web capability is claimed for them. Portable tests cover source bounds, cancellation, usage isolation, redaction, untrusted evidence boundaries, and all three China MAIN compositions. Live Gemini/China smoke is opt-in and credential-gated. See [ADR 0118](adr/0118-hosted-web-search-execution-and-sidecar-routing.md), [ADR 0119](adr/0119-anthropic-hosted-web-search-and-fetch.md), [ADR 0120](adr/0120-gemini-interactions-and-hosted-web-tools.md), [ADR 0121](adr/0121-local-safe-web-fetch.md), and [ADR 0122](adr/0122-china-direct-provider-compatibility-expansion.md) |
| Local Safe Web Fetch | P2 | partial | `WebFetchRequest`/`WebFetchResult`, `WebFetchService`, public-only `aiohttp` transport with pinned A/AAAA validation, manual bounded redirects, HTTPS downgrade rejection, direct/no-proxy/no-cookie GETs, decompressed body limits, text MIME allowlist, charset fallback, non-executing HTML-to-Markdown extraction, secret-in-URL fail-closed handling, redacted `[UNTRUSTED WEB CONTENT]` tool results, provenance, cancellation, and mode-gated composition ownership are implemented. Browser/JS/Playwright/PDF/crawling/auth/cookies/local-network/file/FTP/WebSocket/cache and new providers remain unsupported. See [ADR 0121](adr/0121-local-safe-web-fetch.md) |
| Provider model-discovery strategy | M3/P3A/P3B | partial | Catalog discovery accepts explicit OpenAI-compatible, Anthropic, Gemini, static, or manual-only strategies with protocol-derived behavior retained as a compatibility fallback. Kimi, MiniMax, Qianfan OpenAI-compatible routes, Bailian OpenAI-compatible routes, and TokenHub use bounded inference model paths; GLM and Ark use bounded official/static evidence where a stable inference catalog was not accepted; Qianfan and Bailian Anthropic routes are manual-only because their official compatibility pages do not document a safe `/v1/models` endpoint. Remote availability never promotes model capability or protocol status. Manual model entry remains available, and the TUI uses a credential-free, atomically written bounded catalog cache with classified offline fallback. See [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| Named provider profiles and selection | M2/M3/P3A/P3B | partial | Four explicit wire protocols, Auto/Recommended UI selection that resolves to a concrete protocol, default/one-shot selection, legacy TOML compatibility, redacted `providers list/inspect`, first-run TUI setup, a category-first Settings flow, multi-profile create/edit/save-and-use/confirmed deletion, explicit OpenAI Responses/compatible Chat/DeepSeek/Kimi/GLM/MiniMax/Ark/Qianfan/Bailian/TokenHub presets, service → endpoint variant → protocol → model selection, model-specific protocol validation, user-triggered connection testing with a bounded in-memory remote model picker and manual fallback, and persisted managed defaults through `/provider`, `/model`, and Ctrl+P are implemented. Platform credentials remain separate from normal settings and inherit the existing environment/direct/explicit proxy policy. All P3B routes reuse `OpenAICompatibleProvider`, `OpenAIResponsesProvider`, or `AnthropicProvider`; no four-platform runtime adapters are added. Endpoint/service identity participates in native context affinity, including same-publisher routes. Hosted platform tools remain unimplemented; provider-neutral Web Search and P2 local safe Web Fetch remain unchanged. See [ADR 0123](adr/0123-multi-model-platform-provider-compatibility.md) |
| Typed provider projection boundary | M3 | compatible | `ProviderOption` and `ProviderSelectionResult` are owned by `neuro_code.application.providers.contracts`; Provider application services, bootstrap, and TUI use the canonical seam while profile/session and legacy runtime imports retain identity-preserving compatibility re-exports. Provider lifecycle and selection behavior are unchanged |
| CC Switch compatibility | M2 | partial | Read-only `NEURO_CODE_CC_SWITCH_CONFIG` mapping, three backend formats, loopback `PROXY_MANAGED`, env references, inline-key rejection, and native project override implemented; CC Switch database/process control and its internal failover remain excluded |
| Safe provider failover | M2 | partial | Ordered lazy fallback profiles, first-event commit boundary, monotonic per-run selection, audited failures/selections, `--no-failover`, aggregate errors, opaque-session origin protection, bounded pre-output retry, cooldown circuit breaking, and non-secret in-memory health snapshots are implemented. Persistent health across process restarts remains intentionally absent |
| Typed provider failure taxonomy | M2 | compatible | `ProviderFailure` facts classify authentication, authorization, rate limit, invalid request, model, context, server, timeout, network, protocol, and unknown failures with bounded redacted detail, status, phase, provider/model identity, and bounded `Retry-After`. `ProviderFailurePolicy` keeps retry, circuit, and pre-output failover independent; permanent request/configuration failures do not poison transient health, and output commits disable replay. `ProviderHealth.last_failure_kind` and additive attempt-event fields preserve the existing public fields. See [ADR 0126](adr/0126-provider-typed-failure-taxonomy.md) |
| Provider HTTP proxy policy | M2/M3 | partial | Per-profile environment/direct/explicit-env modes, TUI editing and pre-save validation, in-TUI recovery from a failing managed default during startup, uniform four-adapter HTTPX options, redacted inspection/errors, strict ambiguous-SOCKS diagnostics, and a `socks` installation extra are implemented. Explicit routing was manually verified against DeepSeek while an invalid inherited `ALL_PROXY` remained present; PAC, multi-proxy mounts, and default-bundled SOCKS support remain pending |
| Headless single prompt | M2 | partial | Streaming text/tool loop, plain/JSON/JSONL output implemented |
| Application-owned reasoning effort | M3 | partial | Provider-neutral `low`/`medium`/`high`/`xhigh`/`ultracode`, default `high`, request-only review guidance, typed `ModelContext` propagation, `--effort`, TUI Ctrl+E and `/effort`/`/reasoning`, requested/effective display, and atomic user-preference persistence implemented. Explicit provider dialects retain their own supported reasoning mappings; `ultracode` still falls back to `xhigh` and does not automatically start a workflow. A bounded scope-aware subagent scheduler is available to explicit application callers; automatic Ultracode delegation remains pending |
| Interactive operating modes | M3/M4 | partial | Application-owned `normal`/`accept-edits`/`plan`/`auto`, `Shift+Tab` cycling, `/mode MODE` completion, runtime display, atomic preference persistence, profile/session reapplication, request-only model guidance, and deterministic permission-mode mappings implemented. Plan mode now has a bounded provider-neutral `update_plan` tool, durable session restore/fork inheritance, localized TUI `/plan DESCRIPTION` plus `/view-plan`/`/show-plan`, local `/comment-plan STEP COMMENT` feedback bound to the current plan revision, and an explicit `/execute-plan`/`/run-plan` handoff that records a durable event and an immutable plan-revision snapshot in one opaque session-task lifecycle before the canonical execution request, then moves only to `accept-edits`. Explicit `/schedule-plan`/`/queue-plan` queueing and `/run-task TASK_ID` claiming are implemented with a four-task per-session bound; queued tasks never auto-start or spawn subagents. `auto` is a labelled safe preview using accept-edits defaults unless startup explicitly authorized `--always-approve`; a model safety classifier remains pending, while explicit rules and process sandboxes always remain active |
| Turn and tool timing feedback | M3 | partial | Monotonic time-to-first-actionable-result events per model step, whole-turn completion/failure duration, and local/hosted tool duration are implemented. Calls retain stable call-ID state while consecutive activity is presented as one collapsed group with bounded aggregate summaries. Click or Enter opens a fixed-height, one-call Inline Peek; clicking the open Peek or pressing Escape collapses it, including after focus moves. Up/Down selects a call and Enter opens its Output/Input/Meta Inspector. The Inspector follows live lifecycle updates without blocking Conversation; running timers refresh each Summary group at most once and skip open Peek/Inspector layouts. Conversation never loads artifact output, while Inspector lazily uses the existing session-owned 256 KiB redacted read boundary and reports truncation. Transcript Copy is disclosure-independent. Durations are client observations rather than provider-private reasoning telemetry; durable full tool transcripts remain pending |
| Context-window budget and usage display | M3 | partial | Optional positive per-profile `context_window_tokens`, redacted selection/failover propagation, startup/resume estimation, provider-reported input-plus-output usage events, compact localized status/tooltip display, `/status` reporting, and 50%/80% warning colors are implemented. The field is local metadata and is not sent as an API parameter; exact preflight provider tokenization remains pending |
| OpenAI-compatible/xAI Chat endpoint | M2/P3A | partial | Chat Completions streaming, bounded `max_tokens`, tools, validated native user-image blocks, thinking-mode tool-roundtrip continuity, and fail-closed upstream-import visible-context replay implemented; DeepSeek V4 DSML parsing is enabled only by the explicit `openai-chat` `dialect = "deepseek-v4"` profile. Kimi, GLM, and MiniMax use the same adapter with explicit dialect mappings for current thinking/reasoning controls, tool-choice restrictions, MiniMax `max_completion_tokens`/`reasoning_split`, structured MiniMax reasoning replay, model discovery, and usage/cache fields. Recorded model-specific fixtures and opt-in live tests are present; full native structured-output parity and hosted web remain unclaimed |
| OpenAI Responses/xAI dialect | M2 | partial | Generic portable Responses subset plus optional xAI encrypted reasoning, web/X/code hosted tools, profile-affine native replay, strict legacy official-host fallback, terminal-authoritative output, SSE lifecycle normalization, status stripping, fallback repair, error redaction, explicit hosted-search tool choice, dialect-specific domain filters, and structured nested source/citation extraction are implemented. Standard OpenAI `web_search` is explicit and xAI hosted behavior remains compatible; opt-in live fixtures are present, credential-gated, and allow a model override. Stateful response IDs remain pending; bounded provider-neutral durable compaction records and automatic safe-point compaction are implemented |
| Anthropic provider | M2 | partial | Native Messages SSE, text/reasoning, tools, usage, errors, secret redaction, validated user/tool-result image blocks, conservative hosted Web Search/Web Fetch capability gating, versioned direct-caller server tools, lifecycle events, native continuation replay, bounded `pause_turn`, mixed client/server tool handling, and structured source/citation extraction implemented; live/model-specific fixtures remain opt-in |
| Gemini provider | M2 | partial | Legacy `gemini-generate-content` remains the compatibility adapter with `streamGenerateContent` SSE, text/thoughts, tools, usage, errors, thought-signature round trip, and validated user inline/File API image parts. New `gemini-interactions` owns stable `/v1/interactions` SSE, stateless `store=false` requests, thought/function/builtin steps, native affinity replay, Google Search, URL Context, and mixed hosted/client capability gates; live/model-specific fixtures are opt-in |
| Core tool registry and schemas | M2 | partial | Independent Python contracts plus capability-gated background Bash/task-output/wait-tasks/kill schemas implemented |
| Read/list/grep tools | M2 | partial | Workspace-contained UTF-8 single-file compatibility plus bounded ordered `read_files`, link-safe generated-directory-filtered `list_tree`, and deterministic multi-query `grep_many`; batch output is redacted and byte-bounded, partial read failures remain usable, and independent read-only calls may run in bounded parallel groups while side-effecting and interaction-control calls remain exclusive |
| Search/replace editing | M2 | partial | Atomic exact replacement and containment implemented |
| Bash execution and cancellation | M2/M3 | partial | Bounded foreground output, null stdin, configured provider/proxy credential stripping, timeout/cancel cleanup, POSIX process-group TERM/KILL, fail-closed Windows Job Object creation with atomic `PROC_THREAD_ATTRIBUTE_JOB_LIST` assignment/restricted standard-handle inheritance/descendant wait/whole-Job termination, conversation-scoped managed background start/snapshot/bounded single- and multi-task event waits/timeout/idempotent-kill/binding-switch/application-shutdown lifecycle, bounded model-only completion reminders with task-output/wait-tasks/kill deduplication, restart-aware per-session wake ledgers with bounded cooldown/budget and missing-task reconciliation, and automatic foreground-to-background promotion through the same process tree implemented; persisted global wake default with per-provider inherit/enabled/disabled override and session-scoped `/auto-wake on|off` precedence are implemented; explicit reviewed secret injection remains pending; bounded redacted full-output artifacts for truncated local Bash previews are implemented, while ACP client-terminal and filesystem-reader artifacts remain pending; explicit background auto-wakes may inject one redacted 2 KiB-per-task/8 KiB-per-batch output preview |
| Permission rule precedence | M2/M3 | partial | Deny/ask/allow precedence, per-segment `&&`/`||`/`;`/pipe checks, wrapper peeling, nested `bash -c`, fail-closed complex scripts, asynchronous allow-once/deny, memory-only exact-action session approval, per-ACP-session `session/request_permission` adaptation, path/operation-aware rules, and bounded atomic JSON rule persistence are implemented; local deny/workspace/sandbox decisions remain authoritative and pending tool updates precede client approval. Full rule/file-access grammar and an interactive rule editor remain pending |
| SQLite session event store | M2/M3/M4/M5 | partial | Schema v13, atomic and concurrent-initializer-serialized v1/v2/v3/v4/v5/v6/v7/v8/v9/v10/v11/v12 migrations, profile-affinity and fixed-sandbox metadata, stable titles, synchronized FTS5 documents, namespaced one-to-one external-session aliases, bounded durable structured plans, bounded current-plan comments that are excluded from FTS/export/import and pruned on plan replacement, durable opaque plan-execution task records with an immutable plan snapshot excluded from FTS/export/import and fork copying, one last-safe-terminal execution record per source session, a bounded per-session background-wake ledger, foreign-keyed parent-to-child `subagent_links`, and bounded foreign-keyed `session_compaction_items`. Compaction rows retain only provider/window metadata, source counts and range, an opaque stale-source fingerprint, summary token metadata, creation time, and an already-redacted summary; they are excluded from FTS/export/import and fork copying. The execution record is bound to a persisted `TURN_COMPLETED` event and retains only status, reason code, finalized/recoverable flags, sequence, and timestamp; runtime terminal success commits that event, final ordered session items, synchronized title/FTS, and an optional record through one SQLite transaction, while background auto-wake omits the record so a prior user record is preserved. Execution-record writes are serialized and reject older event sequences or conflicting same-sequence data. Compaction writes are separate short transactions and are not whole-turn atomicity. This is not whole-turn or cross-process atomicity; execution records, compaction items, and subagent links are excluded from FTS/export/import and fork copying. Current-session exact task reads, immutable read-only resume preflight, canonical ordered session-item persistence, message projection, append-only prefix checks, list, resume, fork, export, filesystem-identity workspace matching across platform path aliases, and recursive parent-session deletion of linked child sessions are implemented |
| Session titles and full-text search | M3 | partial | Deterministic ten-word titles from the first visible user prompt, upstream `generated_title` preservation, atomic manual title/FTS updates through `sessions rename` and TUI `/rename`/`/title`, title-weighted SQLite FTS5, multi-token AND-to-OR fallback, cwd filtering, pagination, matched fields, optional snippets, `sessions search`, and workspace-scoped TUI `/sessions QUERY` results implemented; system messages, private reasoning/native items, tool arguments/raw results, and image URLs are not indexed. The live debounced session-picker search is implemented; model-generated titles and the ACP method remain pending |
| Typed session-turn application boundary | M3 | compatible | `RunTurnRequest`, `SessionTurnRunner`, and `SessionTurnService` are owned by `neuro_code.application.sessions.turns`; CLI, TUI, ACP, and application consumers use the canonical seam, while the package and legacy session service retain identity-preserving compatibility re-exports. Turn locking, persisted context, cancellation, event ordering, and Runtime behavior are unchanged |
| Typed session-binding application boundary | M3 | compatible | `ConversationBinding` and `ConversationRunner` are owned by `neuro_code.application.sessions.binding`; ACP, bootstrap, session application, and runtime-facing consumers use the canonical seam, while profile and legacy runtime paths retain identity-preserving compatibility re-exports. Profile-specific session selection and binding replacement, turn locking, cancellation, and Runtime behavior are unchanged |
| Typed session-selection and policy projections | M3 | compatible | `SessionOption`, `SessionSelectionResult`, `ReasoningEffortSelectionResult`, and `InteractionModeSelectionResult` are owned by `neuro_code.application.sessions.contracts`; TUI consumes the canonical projections while profile and legacy runtime paths retain identity-preserving compatibility re-exports. Selection, policy application, locking, resume, and wire behavior are unchanged |
| Typed interactive session-selection seam | M3 | compatible | `SessionSelectionController` and non-owning `SessionSelectionService` are owned by `neuro_code.application.sessions.selection`; TUI uses the facade for list/search/select/rename while `ProfileConversationController` retains locking, binding replacement, workspace checks, resume lifecycle, and the compatibility execution-record projection. Session schema, Runtime, Provider, Finalizer, and wire behavior are unchanged |
| Typed durable session lifecycle boundary | M3 | compatible | `StartSessionRequest`, `ImportSessionRequest`, `RenameSessionRequest`, `ForkSessionRequest`, `DeleteSessionRequest`, and `SessionLifecycleService` are owned by `neuro_code.application.sessions.lifecycle`; Runtime session creation, CLI import/rename, and ACP fork/delete use the canonical requests while the legacy session service delegates with identity-preserving compatibility exports. Workspace visibility, binding replacement, turn locking, protocol cleanup, schema, Runtime execution, Provider, Finalizer, and wire behavior are unchanged |
| Typed session-task query boundary | M3 | compatible | `ListSessionTasksRequest`, `GetSessionTaskRequest`, `SessionTaskQueryController`, and `SessionTaskQueryService` are owned by `neuro_code.application.sessions.task_queries`; Runtime queued-plan lookup and `AgentConversation` task reads use the canonical seam while the legacy session service delegates with identity-preserving exports. Task writes, state transitions, locking, cancellation, Runtime, Provider, Finalizer, schema, and wire behavior are unchanged |
| Typed session-summary query boundary | M3 | compatible | `GetSessionSummaryRequest`, `SessionSummaryQueryController`, and `SessionSummaryQueryService` are owned by `neuro_code.application.sessions.summary`; resume, bootstrap configuration, ACP workspace validation, and session-scoped tool-output artifact reads use the canonical seam while the legacy session service delegates with identity-preserving exports. Lifecycle writes, event/item reads, schema, transactions, Runtime, Provider, Finalizer, and wire behavior are unchanged |
| Typed execution-record query boundary | M3 | compatible | `LoadExecutionRecordRequest`, `LoadExecutionRecordsRequest`, `SessionExecutionQueryController`, and `SessionExecutionQueryService` are owned by `neuro_code.application.sessions.execution_queries`; session catalog and conversation resume/reload reads use the canonical single/bulk seam while the legacy session service retains identity-preserving exports. Execution-record writes, schema, transactions, Runtime, Provider, Finalizer, TUI, ACP, and wire behavior are unchanged |
| Typed session-event query boundary | M3 | compatible | `LoadSessionEventsRequest`, `SessionEventQueryController`, and `SessionEventQueryService` are owned by `neuro_code.application.sessions.event_queries`; session export and session-scoped tool-output artifact reads share the canonical copied immutable mapping projection while the legacy session service retains identity-preserving exports. Event writes, decoding, schema, transactions, Runtime, Provider, Finalizer, TUI, ACP, and wire behavior are unchanged |
| Typed session-item query boundary | M3 | compatible | `LoadSessionItemsRequest`, `SessionItemQueryController`, and `SessionItemQueryService` are owned by `neuro_code.application.sessions.item_queries`; conversation resume/reload and session export share the canonical ordered tuple projection while the legacy session service retains identity-preserving exports. Item writes, plans, comments, events, schema, transactions, Runtime, Provider, Finalizer, TUI, ACP, and wire behavior are unchanged |
| Canonical application consumer imports | M3 | compatible | Bootstrap composition and the TUI import provider/workflow services from their concrete `application.providers.service` and `application.workflows.*` owners; CLI/bootstrap/ACP and CLI serialization import concrete session lifecycle/service/catalog owners. Aggregate package exports remain identity-preserving compatibility paths. This changes only imports, not service ownership, locking, persistence, Runtime, Provider, Finalizer, TUI layout, ACP wire, or workflow behavior; plan/comment/export reads remain with their existing owner because no second production consumer exists |
| Canonical tool-output artifact imports | M3 | compatible | CLI, TUI, ACP, bootstrap, ACP application orchestration, and CLI serialization import the bounded artifact application owner directly from `neuro_code.application.tools.service`; the package aggregate remains an identity-preserving compatibility path. Artifact handles, session visibility, redaction, byte limits, pruning, permissions, storage, Runtime, and protocol behavior are unchanged |
| Rust session import | M2 | partial | Read-only v0/v1 parsing, mixed legacy/current messages, structured images, ordered reasoning/backend-tool payloads, embedded `raw_output`/singular-reasoning recovery with backend-ID deduplication, bounded corrupt-line recovery, atomic SQLite import, built-in sandbox-profile and generated-title preservation with custom-profile rejection, prefix-safe resume, export v4, provider-native image replay, trusted-source xAI Chat replay, and strict-affinity native Responses encrypted/backend-tool replay implemented; compaction and stateful IDs remain pending |
| Full-screen/minimal TUI | M3/M4 | partial | The Textual interface implements a bounded multi-line composer, safe Markdown, stable in-place streaming with scroll-follow protection, an F8 transcript-copy view, and max-116-column conversation blocks on one reading axis. A compact neutral semantic theme uses one interaction accent; consecutive tool calls form one default-collapsed activity group with aggregate summaries, visible failures, elapsed time, truncated Bash intent, and click/Enter access to bounded redacted permissions, full commands, output, artifacts, and diffs. The permanent bottom area is the composer plus one label-free status row with bounded model/effort/mode and context/workspace regions; `/help` and F1 replace the shortcut row. Sparse settings and effort lists separate focus from selection, and modal dialogs use small/medium/large size classes. The existing five-level effort, four interaction modes, plan/task/background-wake workflows, provider/session setup and discovery, English/Simplified-Chinese chrome, fail-closed approvals, cancellation/retry/rewind, viewport reconciliation, durable context, lifecycle cleanup, and cross-platform headless/production-terminal tests remain implemented. Session-scoped bounded Bash output artifacts can be retrieved by the TUI on expansion; artifacts from other output sources remain pending. See [ADR 0108](adr/0108-editorial-tui-presentation.md). |
| Markdown/Mermaid/media rendering | M3/M5 | partial | Safe assistant Markdown uses an application-owned semantic theme, does not interpret model text as Rich/Textual markup, and disables hyperlink activation; headings, emphasis, code, lists, links, and tables are rendered. Tool output and diffs use literal application-styled text. Mermaid and inline media remain pending |

| PTY and process-tree parity | M3/M4 | partial | Foreground/background commands share an owned `ProcessTree`; POSIX waits for a surviving process group, while Windows creates the leader atomically inside a kill-on-close Job Object, restricts inherited standard handles, waits for descendants after the leader exits, and uses handle-based whole-tree termination. A shared interactive-terminal domain/port and manager now provide bounded cursor-addressed tail output, explicit truncation, input/resize/signal/wait/close, permission/workspace/sandbox gating, cancellation-safe creation and registry shutdown. POSIX sessions own a PTY process group; production Windows sessions atomically combine ConPTY and Job-list attributes and terminate the whole Job. Portable attribute/handle/error tests, real Linux PTY tests, native process-tree tests, and opt-in three-platform CLI terminal smoke cover the boundary. ACP additionally supports capability-gated, non-interactive client foreground execution and a bounded standard direct-executable background lifecycle; it does not expose interactive PTY framing/backpressure, input, or resize |
| OS sandbox profiles | M3 | partial | Canonical `off`/`workspace`/`read-only`/`strict` configuration and CLI override, user-profile protection from project weakening, Linux child-scoped Bubblewrap and macOS child-scoped Seatbelt for Bash/background Bash/stdio MCP/enabled-profile PTY requests, private child HOME/TMP and allowlisted environment, request-scoped POSIX inode auditing, explicit close-FD boundaries, read-only edit-schema/runtime gates, profile-specific network enforcement, trusted-helper validation, fail-closed unsupported platforms, dedicated no-skip Linux and ARM64/Intel macOS security CI, native Windows Job/ConPTY CI, and session-fixed profile persistence/resume conflict detection implemented; `off` explicitly provides no OS isolation; macOS detached-descendant lifecycle remains best effort; physical SIP-enabled macOS acceptance is `DEFERRED_NO_MAC_HARDWARE` because no Mac hardware is currently available, which is not a code blocker and does not claim acceptance passed; Windows W3 provides a focused-acceptance-certified provider for enabled non-PTY `workspace` and `read-only` requests; `strict` remains fail-closed because strong read isolation is unavailable. W4 PTY/ConPTY is certified by focused native acceptance, and the W5 workload matrix in CI run `32374860136` passes all 20 measured rows through W3/W4. See [ADR 0111](adr/0111-macos-seatbelt-local-process-sandbox.md), [ADR 0112](adr/0112-windows-appcontainer-sandbox-feasibility-decision.md), [ADR 0114](adr/0114-windows-native-sandbox-runtime.md), and [ADR 0116](adr/0116-windows-developer-workload-compatibility.md) |
| Windows AppContainer sandbox feasibility | M3 | partial | The investigated classic stable unpackaged AppContainer primitives for filesystem/ACL authority, secured named-pipe IPC, runtime, and standard-user execution are viable in isolation. The current stock Git for Windows runtime remains incompatible with the complete non-admin repository workflow without protected-ancestor ACL expansion, a bundled patch, or an unsandboxed broker; those alternatives are rejected. Windows `off` remains supported through the existing Job Object/ConPTY path, which reports `STRONG_DESCENDANT_OWNERSHIP`; enabled `workspace`, `read-only`, and `strict` profiles remain unsupported / fail closed. Evidence PRs #33--#39 are unmerged. See [ADR 0112](adr/0112-windows-appcontainer-sandbox-feasibility-decision.md) |
| Windows native restricted-token and setup authority | M3 | partial | W1 adds the platform-neutral filesystem/network `LocalProcessSecurityCapabilities` axes `READ_ISOLATION`, `WRITE_ISOLATION`, and `NETWORK_ISOLATION`, with explicit `STRONG`/`LIMITED`/`UNSUPPORTED` strengths and fail-closed satisfaction. W1/W2 remain the primitive and setup authorities; their foundation actual capability constant stays `UNSUPPORTED`. The fully wired W3 non-PTY runtime provides the focused-acceptance-certified contract of limited read, strong write, and strong network; a strong-read request cannot be satisfied by the limited provider. Gates 1–5 execute seven native acceptance tests with zero skips. Process lifecycle remains the independent `LocalProcessLifecycleCapability` contract, with the W3 runner Job and existing Job Object/ConPTY paths reporting `STRONG_DESCENDANT_OWNERSHIP`. W2 provisions real `NeuroSandboxOffline`/`NeuroSandboxOnline` local users and persists their SIDs and actual credentials in a DPAPI machine-scope record protected by exact NTFS deny ACEs; the installation-scoped synthetic SID is only a write-only restricted-token principal and is never a read or firewall identity. Native ACL reconciliation uses `SetEntriesInAclW` for canonical deny-before-allow ordering, preserves unrelated controller ACEs, and repairs exact read/primary-user-write/restricting-write/read-only-deny/sensitive-deny tuples. The installation owns a persistent Offline Firewall block scoped only to the real Offline SID; Online identity use does not remove it, and only explicit cleanup removes the rule. Setup reports `READY`/`NEEDS_SETUP`/`NEEDS_REPAIR`/`UNSUPPORTED` and retains an administrator-only setup boundary with non-admin runtime facts. The W5 workload matrix in CI run `32374860136` passes all 20 rows and all HOST/W3/W4 cells without changing this contract. See [ADR 0113](adr/0113-windows-native-restricted-token-sandbox-architecture.md), [ADR 0114](adr/0114-windows-native-sandbox-runtime.md), and [ADR 0116](adr/0116-windows-developer-workload-compatibility.md) |
| Local-process lifecycle capability contract | M3 | compatible | `LocalProcessLifecycleCapability` explicitly separates `STRONG_DESCENDANT_OWNERSHIP` from `PROCESS_GROUP_BEST_EFFORT`; the single satisfaction helper allows strong adapters to satisfy best-effort requests and makes best-effort adapters fail closed before child creation for strong requirements. Linux enabled Bubblewrap and Windows Job Object/ConPTY paths report strong ownership; plain POSIX and macOS Seatbelt report best effort. Filesystem/network authority remains independent. See [ADR 0110](adr/0110-cross-platform-lifecycle-capability-contract.md) |
| ACP stdio/WebSocket | M4 | partial | Official `agent-client-protocol==0.11.0` stdio SDK plus the bounded WebSocket newline-JSON bridge implement protocol-version negotiation, saved client capabilities/info, stable workspace-bound ACP IDs with durable lazy ACP-to-SQLite-ID aliases, session create/list/load/resume/fork/delete/prompt/cancel/close, bounded/redacted replay, fail-closed permissions, session-owned MCP tools, bounded additional directories, capability-gated client text-file reads plus exact replacements, a capability-gated client terminal, bounded inline-image/audio prompts, embedded text and binary prompt blocks, and private artifact/subagent/lifecycle/MCP/compaction extensions. `loadSession: true`, list/delete/fork/resume/close, and MCP `http: true`/`sse: true` capabilities are advertised. When advertised by the client, `read_file` delegates to `fs/read_text_file`; `search_replace` is exposed only when both text read/write capabilities exist and preserves sandbox, workspace, instruction, and permission gates. With `terminal: true` and only when sandbox is `off`, `terminal_exec` sends one direct executable plus arguments through create/wait/output/release; the standard background terminal lifecycle uses opaque task IDs. Image blocks are base64-validated against a raster MIME allowlist and preserve ordered native provider context; audio and binary blocks are bounded and preserved in model context, while unsupported providers receive placeholders. Binary multimedia history replay and interactive client terminal input/resize/PTY framing remain unsupported. Bounded stdio, Streamable HTTP, and legacy SSE servers are accepted; ACP-transport servers remain unsupported. |
| MCP servers | M4 | partial | Official `mcp>=1.28.1,<2` `ClientSession`, schemas, negotiation, JSON-RPC dispatch, tool listing/calls, resources, resource templates, prompts, sampling callbacks, elicitation callbacks, and bounded dynamic refresh serve ACP-provided stdio, Streamable HTTP, and legacy SSE servers. Stdio retains the newline bridge, POSIX process-group, and atomic Windows Job guarantees. Remote endpoints require validated HTTP/HTTPS URLs without embedded credentials or fragments, bounded non-reserved headers, TLS verification, no environment proxy inheritance or redirects, and a 1 MiB response cap. Limits, protected environment rejection, explicit-value redaction, `_meta` omission, collisions, metadata-only ResourceLinks, ASK-over-bypass approval, and idempotent cleanup apply to every transport. Resource/prompt reads and callbacks use private bounded ACP extensions; persistent MCP configuration and ACP transport remain unsupported. |
| Skills and AGENTS.md | M4 | partial | Bounded exact-name `AGENTS.md` inheritance is injected as transient tagged User context, refreshed from a binding-local target before each model step; `search_replace` compares against the last injected path/content snapshot and aborts on unseen changes. Read-only skill discovery scans nested LOCAL roots, every REPO ancestor through the git root, and USER home with deterministic `LOCAL > REPO > USER` shadowing, hard traversal/entry/file/byte/catalog limits, all links/reparse points rejected, and body-sensitive fingerprints. The bounded `skill` tool validates the discovery fingerprint, strips frontmatter, safely samples bundled file names, and supports bounded `$ARGUMENTS`, indexed arguments, and `${SKILL_DIR}` substitution. Alternate instruction names/rules, `.cursor` and vendor-default compatibility, conditional skills, remote/server/bundled skills, hooks/plugins, and subagent integration remain pending |
| Hooks and plugins | M4 | planned | Hooks and plugin marketplace crates |
| Subagents and plan mode | M4 | partial | Durable structured per-session plans are model-managed through `update_plan`, survive resume, inherit on fork, and are viewable in the TUI. Local plan comments, execution handoff, queueing, task claiming, the explicit CLI/TUI/ACP read-only subagent surfaces, lifecycle controls, and parent/child projections are implemented. Every production child-runtime path now receives the actual parent binding capability manifest: CLI opens a parent binding, TUI reads the active binding, and ACP requires an active parent binding. The explicit read-only path treats its fixed tool names as a request and resolves `parent ∩ requested ∩ composition-owned global policy` before binding construction; binding manifests and runtime fingerprints are checked. The bounded application scheduler provides fresh child runtimes, max-parallel limits, retry limits, timeout limits, explicit recursive/depth gates, tool allowlists, read-only/write capability checks, ordered results, and cleanup for every attempt. The arbitrary legacy executor seam is test/internal-only; relationship resume/fork/delete do not recreate runtimes. This proves only `child capability <= actual parent capability`, not the complete permission/workspace/sandbox/MCP/agent security system. Parent-context reuse and automatic Ultracode delegation remain intentionally absent; write-capable child tools remain unavailable |
| Explicit subagent lifecycle CLI controls | M4 | partial | Stage5CZ adds `neuro subagents ACTION TASK_ID --parent-session SESSION_ID` for explicit `resume`, `fork`, and `delete` actions through the application lifecycle owner. Plain and typed JSON projections contain only bounded lifecycle identifiers; the command does not run a model, replay tools, open forks automatically, or read SQLite directly. |
| Explicit ACP subagent lifecycle extension | M4 | partial | Stage5DA adds private `_neuro-code/session/subagents` with strict external `sessionId`, bounded `taskId`, and `resume`/`fork`/`delete` actions. Resume/fork return external ACP aliases and delete returns only a deleted flag; no model turn, tool replay, recursive/parallel child, write tool, internal ID, or child transcript crosses the wire. |
| Fail-closed ACP subagent lifecycle projection | M4 | partial | Stage5DB validates returned parent/task/action correlation and bounds non-delete external aliases before wire serialization. Invalid injected results fail closed; valid lifecycle responses and all runtime/session behavior remain unchanged. |
| ACP subagent alias reconnect compatibility | M4 | partial | Stage5DC bounds lifecycle alias allocation to four attempts, resolves each alias back to the expected child before serialization, retries collisions or wrong-owner results, and preserves the durable alias across repeated resume requests and ACP client reconnects. The private extension, child execution, schema, and standard ACP capability set remain unchanged. |
| Deterministic context-compaction assessment | M5 | partial | Stage5DD adds the canonical `neuro_code.application.memory.compaction` planner with typed usage, soft/hard thresholds, protected-prefix and recent-suffix boundaries, and an index-only bounded plan. Unknown capacity is explicit `UNAVAILABLE`; no summaries, provider calls, durable items, Runtime integration, or session/UI/wire behavior are changed. |
| Provider-aware context summary request boundary | M5 | partial | Stage5DE adds bounded `ProviderContextWindow` metadata and `ContextSummaryRequest` projection in the canonical memory module. Known capacity clamps the summary budget; unknown capacity, non-actionable plans, and empty candidate ranges fail closed. No provider parameter, model call, prompt, durable item, Runtime integration, or interface behavior is added. |
| Provider-aware redacted summary input | M5 | partial | Stage5DF adds `ContextSummaryInputBuilder` and typed bounded projections. It redacts explicit and common credential shapes before control/UTF-8 bounds, omits tool arguments, reasoning, and preserved provider payloads, and applies an injected local token estimator against the provider window's remaining budget. It does not call a Provider, build a prompt, mutate context, persist compaction items, or change Runtime/interface behavior. |
| Provider-backed bounded context summary generation | M5 | partial | Stage5DH adds `ProviderContextSummaryGenerator` and `ContextSummaryGenerationResult`. It accepts only the redacted bounded input, validates provider/model affinity, builds a temporary prompt, performs exactly one `DISABLED` no-tool request, buffers deltas, prefers `ModelCompleted.response_text`, redacts/bounds the result, and fails closed on missing/empty/multiple completion or remote tool events. It does not enable automatic compaction, retry, persistence, events, or whole-turn atomicity. |
| Memory and compaction | M5 | partial | Stage5DD establishes deterministic assessment; Stage5DE/DF add provider-aware bounded request/input contracts; Stage5DG adds durable compaction items and explicit resume reconstruction; Stage5DH adds one-request strict no-tool summary generation; Stage5DI adds an explicit application persistence service with pre-call source-fingerprint validation and `SessionStore` delegation; Stage5DJ fixes the compatibility boundary: compaction rows stay out of export/import and fork copying, while `finalize_turn` remains atomic only for its own event/items/search/execution-record transaction. Automatic Runtime triggering and bounded events are enabled only under ADR 0107's controlled, persisted, known-capacity conditions; Provider generation and compaction persistence remain separate transactions. |
| Explicit context-compaction trigger | M5 | partial | Stage5DK adds `ContextCompactionTriggerService` as a default-disabled application boundary. `DISABLED` performs a pure assessment; `EXPLICIT` delegates only an actionable plan after the caller supplies stale-source metadata. The trigger does not change Runtime, Provider, SessionStore schema, export/import, events, CLI, TUI, or ACP behavior and does not reuse ordinary turn budgets. See [ADR 0091](adr/0091-explicit-context-compaction-trigger.md). |
| Runtime compaction safe-boundary gate | M5 | partial | Stage5DL adds a typed gate with `BEFORE_MODEL_REQUEST`/`AFTER_TOOL_BATCH` safe points, fail-closed active-operation/cancellation checks, and a separate one-request/no-tool budget contract. It does not integrate `AgentRuntime`, add events, enable automatic triggering, implement timeouts, or claim whole-turn transactionality. See [ADR 0092](adr/0092-runtime-compaction-safe-boundary.md). |
| Enforced Runtime compaction timeout | M5 | partial | Stage5DM adds a finite, enforced wall-clock limit (30 seconds by default, 300 seconds maximum) around an allowed explicit compaction request, covering summary generation and persistence. Timeout raises `ContextCompactionTimeoutError`; Provider errors, storage errors, and cancellation remain unchanged. It does not integrate `AgentRuntime`, enable automatic compaction, or claim cross-operation transactionality. See [ADR 0093](adr/0093-enforced-context-compaction-timeout.md). |
| Runtime compaction failure projection | M5 | partial | Stage5DN adds `classify_context_compaction_failure()` with a bounded timeout projection to recoverable `BUDGET_LIMITED/WALL_TIME_BUDGET` (`finalized=false`) and a `TURN_FINALIZATION` record policy. Cancellation, Provider, and storage failures remain propagation-only; unknown exceptions are unclassified. The projection stores no error details, does not change `AgentRuntime`, emit events, enable automatic compaction, or claim cross-operation atomicity. See [ADR 0094](adr/0094-runtime-compaction-failure-projection.md). |
| Explicit Runtime compaction seam | M5 | partial | Stage5DO adds an optional, default-disabled `AgentRuntime.compaction_runtime_gate` and `trigger_context_compaction()` for a caller-owned complete safe-boundary request. Missing configuration fails closed; delegation does not mutate context, ordinary turn steps, events, or execution records. `AgentRuntime.run()`, Composition, automatic triggering, and production gate wiring remain unchanged. See [ADR 0095](adr/0095-explicit-runtime-compaction-seam.md). |
| Application-owned explicit compaction caller | M5 | partial | Stage5DP assembles one redaction-aware `ContextCompactionRuntimeGate` per `ApplicationComposition` binding and injects it into `AgentRuntime` without enabling automatic triggering. `AgentConversation.trigger_context_compaction()` is the explicit caller, serializes compaction with the existing session turn lock, requires a matching persisted session for `EXPLICIT` requests, and delegates an immutable caller-owned snapshot. Transcript, events, ordinary turn budgets, and `finalize_turn()` transactionality remain unchanged. See [ADR 0096](adr/0096-application-owned-compaction-caller.md). |
| Atomic turn finalization with compaction | M5 | partial | Stage5DQ adds opt-in `SessionStore.finalize_turn_with_compaction()`; SQLite atomically commits the completion event, session items/search projection, optional execution record, and one durable compaction row. Ordinary `finalize_turn()` and `save_compaction_item()` remain unchanged, provider generation is outside the transaction, and no Runtime or automatic compaction caller uses the new boundary yet. See [ADR 0097](adr/0097-atomic-turn-finalization-with-compaction.md). |
| Turn-recorder compaction finalization owner | M5 | partial | Stage5DR lets `TurnEventRecorder.finalize_turn_completion()` opt into the atomic storage method with a prevalidated `DurableCompactionItem`; ordinary calls remain unchanged, invalid input fails before in-memory completion append, and no current Agent loop or automatic trigger supplies the item. Provider generation and failure projection remain caller-owned. See [ADR 0098](adr/0098-turn-recorder-compaction-finalization-owner.md). |
| Typed compaction turn projection | M5 | partial | Stage5DS adds `ContextCompactionTurnProjection`: successful triggers transfer only the persisted validated item; timeout carries a bounded recoverable budget outcome; cancellation, Provider, storage, and unknown failures are not silently consumed. No events, persistence, automatic compaction, or normal-loop behavior are enabled. See [ADR 0099](adr/0099-context-compaction-turn-projection.md). |
| Explicit compaction turn owner | M5 | partial | Stage5DT adds an opt-in `TurnEventRecorder.finalize_turn_from_compaction_projection()` seam. Successful projections require the caller's ordinary outcome and use atomic turn-plus-item storage; timeout consumes only its bounded outcome; no-op and propagation-only projections fail closed. The normal loop and automatic compaction remain unchanged. See [ADR 0100](adr/0100-explicit-compaction-turn-owner.md). |
| Application compaction owner under the turn lock | M5 | partial | Stage5DU adds `AgentConversation.run_context_compaction_with_owner()` and the matching `ConversationRunner` protocol method. It validates the caller-owned immutable request, serializes the gate and owner under the existing session turn lock, consumes only successful or controlled-timeout projections, and fails closed before the owner for no-op projections. Cancellation, Provider, storage, and unknown failures propagate unchanged; the normal loop, automatic compaction, events, and UI remain unchanged. See [ADR 0101](adr/0101-application-compaction-owner-under-turn-lock.md). |
| Context usage snapshots and stale-source request construction | M5 | partial | Stage5DV adds side-effect-free application helpers that use provider-reported input/output usage when available, bounded `ModelContext` estimation otherwise, retain unknown provider capacity as unknown, and compute a stale-source fingerprint only for an actionable explicit request. They do not call Provider/storage, mutate context, or enable automatic compaction. See [ADR 0102](adr/0102-context-usage-snapshot-and-stale-source-builder.md). |
| Explicit live-context compaction command | M5 | partial | Stage5DW adds `AgentConversation.run_explicit_context_compaction_with_owner()`. It builds the live guided context under the existing session lock, delegates usage/stale-source construction to the configured Runtime gate, allocates bounded identity/time metadata, and reuses the typed owner projection. It requires a persisted session and remains explicit: no normal-loop integration, automatic trigger, event, transcript mutation, or shared Provider/storage transaction. See [ADR 0103](adr/0103-explicit-live-context-compaction-command.md). |
| Explicit compaction command projection | M5 | partial | Stage5DX adds `ContextCompactionCommandResult` and bounded CLI/ACP serializers. `completed`, `not_needed`, and controlled `budget_limited` timeout results expose only opaque IDs, counts, and summary-token metadata; Provider, cancellation, storage, and unknown failures remain exceptions. No command, event, normal-loop integration, or automatic trigger is enabled. See [ADR 0104](adr/0104-explicit-compaction-command-projection.md). |
| Unified ordinary execution budget and replan guidance | M3/M5 | partial | Formal CLI/TUI/ACP entrypoints default to the application-owned `normal` 48/48/192 model/tool-round/tool-call profile, while `deep` selects 96/96/384. Legacy `--max-steps N` now scales all count-based ordinary limits to N/N/4N; Finalizer attempts remain independent. In controlled mode, batch-ending `REPLAN` decisions inject non-persistent synthetic guidance until new progress, and every request receives provider-neutral batch-first guidance. Existing sequential tool execution and stuck detectors remain unchanged. See [ADR 0105](adr/0105-unified-execution-budget-and-replan-guidance.md). |
| Bounded long-task Runtime | M3/M5 | partial | Controlled turns now expose typed budget pressure to the model and bounded interface events, automatically compact at safe boundaries when a persisted session has a known Provider context window, and emit bounded progress segment checkpoints without resetting the global execution budget. The standard TUI deliberately omits raw execution counters. Synthetic budget/checkpoint guidance and compaction requests stay out of the canonical transcript. Compaction Provider generation and persistence are separate operations, and segment checkpoints are audit markers rather than crash-recovery or workspace rollback checkpoints. Automatic delegation remains absent. See [ADR 0107](adr/0107-bounded-long-task-runtime.md). |
| LSP, worktree, checkpoints | M5 | planned | Tools/workspace/worktree crates |
| Voice, image, video, web tools | M5 | planned | Provider-backed adapters required |
| Leader, relay, Computer Hub | M5 | planned | Local pieces plus external service boundary |

## ACP private extension

The private namespaced `_neuro-code/session/artifacts` extension is now
implemented for bounded, session-associated list/read access to redacted local
tool-output artifacts. It is not advertised as a standard ACP capability;
other extensions remain unsupported.
