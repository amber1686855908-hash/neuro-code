name: Windows AppContainer Runtime POC2B (evidence only)

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/windows-appcontainer-runtime-poc2b.yml"
      - "scripts/evidence/windows_appcontainer_runtime_poc2b.py"
      - "scripts/evidence/windows_appcontainer_runtime_poc2b_bootstrap.c"

permissions:
  contents: read

jobs:
  appcontainer-runtime:
    name: AppContainer Runtime (${{ matrix.runner }})
    strategy:
      fail-fast: false
      matrix:
        runner: [windows-2025, windows-2022]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v7.0.0
      - uses: astral-sh/setup-uv@v8.3.2
        with:
          version: "0.11.29"
          python-version: "3.12"
          enable-cache: true
      - run: uv sync --extra dev --locked
      - name: Compile evidence-only native runtime bootstrap
        shell: pwsh
        run: |
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $installation = & $vswhere -latest -products * `
            -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 `
            -property installationPath
          if (-not $installation) {
            throw "Visual C++ toolchain not found"
          }
          Import-Module "$installation\Common7\Tools\Microsoft.VisualStudio.DevShell.dll"
          Enter-VsDevShell `
            -VsInstallPath $installation `
            -SkipAutomaticLocation `
            -DevCmdArguments "-arch=amd64 -host_arch=amd64"
          cl.exe /nologo /W4 /WX /MT /DUNICODE /D_UNICODE `
            scripts/evidence/windows_appcontainer_runtime_poc2b_bootstrap.c `
            /Fe:"${env:RUNNER_TEMP}\windows_appcontainer_runtime_poc2b_bootstrap.exe" `
            /link advapi32.lib userenv.lib ws2_32.lib `
            2>&1 | Tee-Object -FilePath "${env:RUNNER_TEMP}\appcontainer-runtime-poc2b-build.txt"
          if ($LASTEXITCODE -ne 0) {
            throw "Native evidence child compilation failed with $LASTEXITCODE"
          }
      - name: Execute AppContainer runtime and standard-user gates
        shell: pwsh
        run: |
          uv run python scripts/evidence/windows_appcontainer_runtime_poc2b.py `
            --bootstrap "${env:RUNNER_TEMP}\windows_appcontainer_runtime_poc2b_bootstrap.exe" `
            --report "${env:RUNNER_TEMP}\windows-appcontainer-runtime-poc2b.json"
      - name: Upload evidence artifact
        if: always()
        uses: actions/upload-artifact@v4.6.2
        with:
          name: windows-appcontainer-runtime-poc2b-${{ matrix.runner }}
          path: |
            ${{ runner.temp }}/appcontainer-runtime-poc2b-build.txt
            ${{ runner.temp }}/windows-appcontainer-runtime-poc2b.json
          if-no-files-found: error
